<!-- Source: English user manual 1.48 / App 1.2.3; machine-translated to Japanese -->
# ホームギャラリー ユーザーマニュアル

| 項目 | 値 |
|------|------|
| マニュアルバージョン | 1.48 |
| アプリのバージョン | 1.2.3 |
| 作成日 | 2026/09/20 |

---

## 1. はじめに

**ホームギャラリー**（家相簿）は、Android用の写真・動画管理アプリで、**デバイス内**のメディアや**家庭内ストレージ**（NAS / SMB経由でPC）に対応しています。**スペースは自分で選べます—追加のクラウド容量を購入する必要はありません。** 既に持っているディスクを使い、写真は自分が管理するストレージに保存されます。

- **ローカルギャラリー**で写真や動画を閲覧・管理
- **ホームアルバム**を共有フォルダーに接続する（各カードは1つのホスト＋1つの共有＋1つのフォルダー）
- 設定の **Host connections** で SMB の資格情報を一度登録する（一度スキャンして、一度サインインし、後で再利用）
- 写真を表示、編集、共有、コピー、移動、名前変更、削除する
- **スライドショー** は選択した写真（ローカル / ホームアルバム）を自動再生します；**Pro** はスライドショーをテレビにキャストすることもできます
- よく使うフォルダーをブックマークする
- ローカルの**タイムライン**を閲覧する；**ホームアルバムタイムライン**で日付ごとにホームメディアを閲覧する
- **ホームビデオ**は電話でストリーミングできます（最初にファイル全体をダウンロードする必要はありません）
- **UI言語:** 英語、繁体字中国語、簡体字中国語、日本語、韓国語（システム言語に従う）

このアプリはクラウドギャラリーを**ホストしません**。SMB のパスワードはデバイスに保存されます。**Basic** は完全で広告のない写真管理アプリです（スライドショー、ホームビデオストリーミング、タイムライン、ごみ箱、並行フォルダ、**手動**アルバムバックアップ、**バックアップゾーンの閲覧**など）。**Pro**（任意、一度きりの購入）では、さらに**自動**アルバムバックアップおよびTVへの**写真／スライドショーキャスト**が解除されます。

**要件:** Android 11 (API 30) 以上。

---

## 2. はじめに

### 2.1 アプリを開く

アプリは**ローカルギャラリー**で開きます。まだ写真へのアクセスが許可されていない場合は、**写真と動画**を許可してください。

### 2.2 メインナビゲーション（下部バー）

**ボトムナビゲーションバー**は主要な領域を切り替えます（**横向き**では同じ項目でサイドレールになります）。タブは4つあります：

| タブ | その機能 |
|------|------|
| **ローカル** | デバイス内ギャラリー（フォルダ / タイムライン）。**ごみ箱**が有効になっている場合、ローカルギャラリーのタイトルバーから開きます |
| **ホーム** | 最後に開いたホームアルバムを開きます；もう一度タップすると **アルバムピッカー** を開く／閉じる（アルバムの切り替え、ホームアルバムの管理） |
| **ブックマーク** | ブックマークパネルを開く／閉じる：フォルダブックマークおよび**ビデオブックマーク**；タップしてジャンプまたは再開
| **その他** | メニュー: **アルバムのバックアップ**（無効の場合は設定の切り替えに移動；有効の場合はバックアップを開き、保留中／失敗のバッジが表示される可能性あり）、**設定**、**情報** |

> サイドバーは**ありません**。また、「ネットワークストレージ」の接続リストもなく、ホームのファイルは常に**ホーム**タブから開きます。
> **「について」**（評価 / 共有 / バージョン）は **その他 → について** にあります。設定 → サポート でも開けます。Pro にアップグレードと問題の報告も設定にあります。

**ホーム / ブックマークを2回目にタップ：** すでにそのタブにいる場合、同じアイコンをタップすると、閲覧中のページを離れずにピッカーパネルを切り替えることができます。

マルチセレクト中やビューア／プレーヤーで、下部バーが隠れ、ブラウジングに戻ると再表示される場合があります。

### 2.3 アプリを終了する

ローカルギャラリー、ホームアルバム、または設定などのメイン画面では、最初の **戻る** ボタンの押下時に「もう一度戻るを押すと終了します」と表示されます。2秒以内にもう一度押すとアプリが閉じます。

---

## 3. 地元のギャラリー

### 3.1 写真の閲覧

- **ルート**はフォルダー（例えば DCIM、Pictures）を表示します
- フォルダーを開いて写真のグリッドを表示する
- 写真をタップして**全画面表示**
- **ごみ箱**が有効になっている場合: タイトルバーを開くと**ごみ箱**が開きます（アイテムがあるとカウントバッジが表示されます; Basic と Pro は設定でこれを有効化できます）

### 3.2 検索と並べ替え

- 上の検索フィールドを使って**ファイル名**で検索します；右側の**X**はテキストがある場合はクリアし、空の場合は検索を閉じます
- 並べ替えをタップして、フィールド（名前、変更日、撮影日、サイズ、フォルダ）および昇順／降順を変更します

### 3.3 複数選択

1. 写真や動画を**長押し**して選択モードに入ります。**フォルダ**のルートでは、フォルダを長押しすると内部のすべてのメディアを選択できます。
2. 他のアイテムをタップして追加します（フォルダモードでは、フォルダ全体を選択するかどうかを切り替えるためにフォルダをタップすることもできます）
3. **範囲選択:** すでに選択している状態で、別の項目（またはフォルダ）を**長押し**すると、現在の閲覧順でその間の**すべて**の項目（含む）が選択されます。アンカーは、選択を開始した最後のタップまたは長押しです。
4. 下部ツールバーには以下が表示されます：**スライドショー**、**コピー**、**移動**、**削除**、**共有**、**ホームアルバムにアップロード**
5. フォルダモードでフォルダ内のメディアをすべて削除した後、パスにファイルが残っていない場合、アプリは**空のフォルダを削除しようとします**（`DCIM` / `Pictures` のようなシステムルートは削除されません。メディア以外のファイルを含むフォルダは保持されます）

> **スライドショー:** 複数選択の後、選択した**写真**を自動再生するには**スライドショー**をタップします（動画はスキップされます）。§5.8を参照してください。

### 3.4 ブックマーク

ギャラリーツールバーの**ブックマーク**アイコンをタップして、現在のフォルダーをブックマークします。後で**ブックマーク**タブから開くことができます。

### 3.5 ブラウズモード

地元のギャラリー **root** では、ツールバーで表示を切り替えます:

| モード | 表示される内容 |
|------|------|
| **フォルダー** | フォルダーごとに閲覧（デフォルト）；ルートはまずフォルダーを表示し、その後に写真/ビデオのグリッドを表示 |
| **タイムライン** | すべてのメディアを **撮影日** 順（最新が先）で、日ごとにグリッド表示 |

**タイムライン：**

- 下の**日付軸:** 年／月／日単位；ピンチして変更するか、ヒントの「年／月／日」をタップしてください
- 日単位の粒度では、軸にはサムネイルが表示されます；ラベルは日付（中国語のロケールでは `M月d日`）と年です
- 右側の**高速スクロールバー**（コンテンツが十分にある場合）は素早くジャンプします
- 検索、複数選択、ビューア、およびビデオ再生がサポートされています

**共有:**

- ブックマークはまだフォルダのパスを指しています；ブックマークを開くとフォルダモードに戻ります

**設定 → ローカルギャラリー → グリッド表示** は、デフォルトの閲覧モード（フォルダー / タイムライン）とデフォルトの並べ替えを設定します。アプリは最後に使用したモードも記憶します。

---

## 4. ホームアルバムとSMBホスト

> **PCやNASでフォルダーをまだ共有していませんか？** スタンドアロンのセットアップガイドから始めましょう:
> [Set up home storage](./setup-home-storage/) ([Windows 11](./setup-home-storage/windows-11.md) · [Synology](./setup-home-storage/synology.md) · [QNAP](./setup-home-storage/qnap.md); 接続できない場合は [Troubleshooting](./setup-home-storage/troubleshooting.md) を参照; **離れて NAS** は [WebDAV](./setup-home-storage/webdav.md) を参照; **離れて Windows / WebDAV がない場合** は [Tailscale](./setup-home-storage/tailscale.md) を参照).

ホームファイルは2つの層を使用します:

| コンセプト | 意味 |
|------|------|
| **SMB ホスト** | 1 NAS / PC と資格情報（設定 → **ホスト接続** に登録済み） |
| **ホームアルバム** | 1枚のカード = そのホスト上の**1**つのシェア + その中の1つのフォルダパス |

### 4.1 SMB ホストを追加

1. **その他 → 設定 → ホスト接続 → ホスト接続の管理** を開きます
2. **SMBホストを追加**をタップ（またはホームアルバムのフローでは「新しいSMBホストを探す」）
3. ダイアログはLANをスキャンし、あなたに**Wi‑Fiがオンであり、ストレージと同じネットワーク上にあること**を確認するよう求めます
4. ホストを選択（すでに保存されているホストは薄く表示されます）→ 資格情報を入力；ホスト名は利用可能な場合、表示名として使用されます → **接続をテスト** → **保存**
- **LAN IPv4:** 家庭用 Wi‑Fi（例えば `192.168.x.x`）
- **リモートアクセス**（任意、§4.4参照）：
- **WebDAV:** NAS の間離れている場合；**外部ホスト (DDNS / 公開IP)** と **HTTPS ポート** を入力（多くの場合 `5006`）
- **Tailscale:** Windows用、またはWebDAVが利用できない場合; `100.x`を入力してください
- **なし:** ホームLANのみ、外部ヘルパーなし
5. ホストアドレスを手動で入力することもできます

**接続のメモ（設定内）**

- ホームアルバムは **同じホーム Wi‑Fi（LAN）** 用です；自宅ではアプリは **SMB** を使用します
- 外出先でモバイルデータを使用する場合:
- **NAS:** **WebDAV** を好む (DDNS / 公共IP); [WebDAV guide](./setup-home-storage/webdav.md) を参照
- **Windows PC** またはなし WebDAV: **Tailscale** または既存の LAN VPN を使用; [Tailscale guide](./setup-home-storage/tailscale.md) を参照
- UIには「ホームLAN」や「外出中」と表示されることがあります。外出中モードでも、WebDAV、Tailscale、またはVPNが正しく設定されている必要があります。
- 「接続ノート」カードは、ホスト接続の管理でオンラインの **WebDAV / Tailscale** 設定ページを開きます
- **Enable IPv6**(同じページ、**デフォルトでオフ**):オフ時はDiscovery and ConnectがIPv4のみを使用し、IPv6フィールドは非表示;保存されたIPv6は保持されます。オンにしてスキャン/IPv6を使用します。AwayはWebDAV / Tailscale(§4.4参照)を使用。

### 4.2 ホームアルバムを追加する

1. 下部ナビゲーション → **ホーム**（アルバムがまだない場合、設定が提案されます；または再度 **ホーム** をタップ → **ホームアルバムを管理**）
2. まだアルバムがない場合は、**ホームストレージに接続**（または **追加** FAB）をタップします
3. **まだホストがいない場合:** 「ホストを追加 SMB」ダイアログが開きます。保存後、セットアップが続行されます
4. **既にホストが存在する場合：**
- 1つのホスト：**共有を選択** に進む
- 複数のホスト：ホストを選択してから、共有を選択
- **新しいSMBホストを見つける** はスキャンが必要なときに利用可能です
5. **アルバムのフォルダーを選択:** 共有内のサブフォルダーを参照します; アルバムのルートで任意のレベルで停止できます
6. **表示名（任意）** はフォルダ名がデフォルト → **完了** をタップ（これで **1つ** のアルバムが作成されます）
7. **ホーム**タブから開きます；アルバムを切り替えるには再度**ホーム**をタップします

＞ セットアップは **もう** フルホストスキャンを再実行したり、複数の共有を一度に選択して多くのアルバムを作成したりすることはできません。

### 4.3 閲覧と管理

1. 下部ナビゲーション → **ホーム** → アルバムを開く（または **ホーム** を再度タップしてピッカーで切り替える）
2. そのソースフォルダーを閲覧する（グリッド / リスト）
3. タイトルバーは **フォルダ** / **タイムライン** を切り替えます。タイムラインはフォルダを横断して日付ごとに写真や動画を表示します（**Basic**）。初めて開くとインデックスが作成され、後の訪問ではキャッシュが優先されます。インデックスが約24時間以上古い場合、接続後に自動的に静かに更新されます。また、手動で更新することもできます。タイムライン上では、現在表示されている付近のフォルダを更新するために **下に引き下げる**（セグメント更新）ことができ、ライブラリ全体の再スキャンは不要です。
4. サブフォルダ内でタイムラインに切り替えると、**そのフォルダの範囲のみ**が表示されます
5. 写真をタップして表示します。ツールバーで**更新**できます。タイムラインで**名前を変更**した後、インデックスは新しい名前に従います。その後の**移動**では新しいパスが使用されます
6. タイムラインのマルチセレクトは、**時間を揃える**（参照写真と一致させる）ことや、**単一の写真の時間を修正する**（日付と時刻）ことができます
7. ホームアルバムの**管理**画面では、各アルバムに**タイムラインスキャン深度**（1～8レベル、デフォルトは4；アルバムのルートはレベル0）があります

**アルバムのリンクを削除する**

- ホームアルバムの**管理**画面で：カードを長押しするか ⋮ をタップ → **アルバムリンクを削除**
- これはアプリ内のリンクのみを削除します；ストレージデバイス上のファイルは**削除されません**

**複数選択して移動／コピー**

- 宛先ピッカーはホームアルバムを表示名のみで一覧表示します（2行目のパスは表示されません）、その後サブフォルダを入力できます

### 4.4 離れた場所からのアクセス（オプション）

家を出た後Wi‑Fi、ホストタイプのためのリモート方法を1つ選んでください:

| ホストタイプ | リモートアクセス | 備考 |
|----------|----------|------|
| **NAS** (HTTPS WebDAV 有効) | **WebDAV** | DDNS / 公開IP + ポート; [webdav.md](./setup-home-storage/webdav.md) を参照 |
| **Windows PC** / 該当なし WebDAV | **Tailscale** | Enter `100.x`; 下記および [tailscale.md](./setup-home-storage/tailscale.md) を参照 |
| すでに OpenVPN などを介して LAN に接続済み | **なし**（または LAN アドレスを保持） | 通常は追加の WebDAV / Tailscale は不要 |

#### WebDAV（NASに推奨）

1. ホームWi‑Fiで、まずホストとホームアルバムを設定する（§4.1–4.2）
2. NASで**WebDAV HTTPS**を有効にし、**DDNS**またはパブリックIPを準備します
3. ホストを編集 → **リモートアクセス = WebDAV** → 外部ホストとポートを入力 → **接続をテスト** → **保存**
4. （ほとんどの家庭用NASは自己署名証明書を使用しています）**信頼されていないHTTPS証明書を許可**を有効にすることができます：アプリはまず**システムの信頼**を試みます；それが失敗した場合、**そのホストの証明書をピン留めします（TOFU）**—最初の接続に成功した時にフィンガープリントが保存されます。後でNAS証明書が変更されて接続に失敗した場合は、このオプションを一度オフにしてから再度オンにしてピンを再設定してください
5. 不在時にホームアルバムを開く（確認のためホームWi‑Fiをオフにする）

完全な手順とセキュリティに関する注意事項: [setup-home-storage/webdav.md](./setup-home-storage/webdav.md)。

#### Tailscale（Windows / WebDAVなし）

**Tailscale** を安全なトンネルとして使用し、携帯電話が **`100.x`** で自宅 SMB に接続できるようにします。

**短いフロー:**

1. ホームWi‑Fiで、まずホストとホームアルバムを設定する（§4.1–4.2）
2. **PC / NAS:** Tailscale をインストール → サインイン → 接続済み → **そのマシンの Tailscale IPv4 (100.x) をコピー**
3. **電話:** インストール Tailscale → **同じアカウント** → 接続済み
4. 外出中にホームアルバムを開く → プロンプトが表示されたら **開くTailscale** → PCの`100.x`をコピー → アプリに戻る → **貼り付け** → **Tailscale IPを保存して再試行**

**日常的な使い方:** アルバムが必要なときにTailscaleを開きます。アプリを離れるかホームに戻るとWi‑Fi、アプリは電力を節約するためにTailscaleの切断を求めます。すでにLANにOpenVPN（または同様のもの）がある場合、通常はTailscaleは必要ありません。

完全なウォークスルー、FAQ、ノート：[setup-home-storage/tailscale.md](./setup-home-storage/tailscale.md)。

#### IPv6 を有効にする（オプション）

**ホスト接続**の下で、**有効化 IPv6**（**デフォルトではオフ**）を切り替えることができます：

- **オフ:** 発見と接続はIPv4のみ; 編集時にIPv6フィールドは非表示; 保存されたIPv6は保持される
- **オン:** アプリはIPv6をスキャン/入力でき、接続候補間で使用できます
- 遠隔アクセスには §4.4 の **WebDAV / Tailscale** を使用してください

---

## 5. 写真ビューア

ローカルまたはホームギャラリーの写真をタップして全画面表示します。他のアプリ（ファイル、Download、メッセージなど）でも**Home Galleryで開く**ことができます。

### 5.1 ジェスチャー

| アクション | それが行うこと |
|------|------|
| 左にスワイプ / 右にスワイプ | 前の写真 / 次の写真 |
| ピンチ | ズームイン / ズームアウト (約1×–5×) |
| 拖動 | ズーム後にパン |
| ダブルタップ | 1×と2.5×の切り替え |
| 写真をタップ | フルスクリーン切り替え（ツールバーを表示 / 非表示） |

左下隅には写真の日付が表示されます: EXIF **DateTimeOriginal / Digitized** が存在する場合、**日付 + 時刻** が表示されます。存在しない場合は、修正日 / 作成日の**より早い方**が使用され、**日付のみ** が表示されます（通常の DateTime タグは無視されるため、誤って後のファイル時刻が使用されることはありません）。フルスクリーンまたはスライドショー時は非表示です。

### 5.2 上部ツールバー

| ボタン | 機能 |
|------|------|
| ← 戻る | アルバムに戻る |
| ファイル名 | 現在の写真名 |
| 削除 | 現在の写真を削除する（確認） |
| 編集（鉛筆） | エディターを開く（**一部の形式のみ**、§5.6参照） |

> 現在のフォーマットを編集できない場合、**編集**ボタンは表示されません。

### 5.3 サイド／ボトムアクション

矢印をタップしてアクション行を展開します（自動折りたたみモードの場合）、または設定で**常に表示**に設定します。

| アクション | それが行うこと |
|------|------|
| 移動 | 宛先フォルダーを選択した後に移動 |
| コピー | 目的のフォルダーを選んだ後にコピー |
| Proのプロパティ | ファイル情報とEXIF |
| 名前の変更 | ファイル名を変更する |
| 共有 | 別のアプリで共有 |

> 縦向き：下部に操作。横向き：右側に操作。

### 5.4 Proプロパティ

プロパティダイアログには次の内容が表示されます:

- **ファイル情報:** 名前、パス、サイズ、種類、作成日、最終更新時間
- **EXIF**（存在する場合）：解像度、撮影設定、GPS、など

EXIFのない写真でもファイル情報は表示されます；**作成日**は常に表示されます。

### 5.5 コピー / 移動

1. コピーまたは移動をタップ
2. **フォルダブラウザ**（全画面グリッド）で目的地を選択する
- 空のフォルダーはデフォルトで**非表示**になっています。ファイルがまだないフォルダーを選択するには、**空のフォルダーを表示**をオンにしてください
- 同じ行に **新しいフォルダ** と **確認** があります
- 「新しいフォルダー」の後、それが対象になります（空でも表示されるので、すぐに確認できます）
3. もし宛先に同じ名前が既にある場合:
- **上書き:** 新しいファイルで置き換える
- **両方を保持する:** 元のファイルを保持する; 新しいファイルには番号（`photo (1).jpg`）が付く
- **キャンセル:** 中止

ローカルマルチセレクト: **移動**は写真と動画で機能します; 選択に動画が含まれる場合、**コピーは非表示**です（動画のコピーはまだ実装されていません）。

### 5.6 画像形式と制限

アプリは拡張子とMIMEタイプを使用して、ファイルが**表示**可能か**編集**可能かを判断します。この表は**ローカルギャラリー**および**ホームアルバム**に適用されます（ホームストレージ上のその他のファイルは通常のファイルとして表示され、ビューアで開くことはできません）。

#### 表示可能な写真形式

`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.bmp`, `.heic`, `.heif`, `.svg`, `.svgz`

#### 編集可能な写真形式

これらの形式のみがビューワーから**編集**に入ることができ、保存後も同じ形式のままです:

| フォーマット | 注意 |
|------|------|
| `.jpg` / `.jpeg` | 完全にサポートされています |
| `.png` | 完全にサポートされています |
| `.webp` | 完全にサポートされています |

他の表示可能な形式（`.gif`、`.bmp`、`.heic`、`.heif`、`.svg`、`.svgz`など）には**編集ボタンがありません**ので、保存しても形式を変更したりファイルを損傷したりすることはできません。

#### SVG / SVGZ

| トピック | ノート |
|------|------|
| 表示 | サポートされています。ベクトルはビットマップに**ラスタライズ**されます |
| ズーム | 極端なズームは、ネイティブのベクタービューアよりも鮮明でないように見えることがあります |
| 編集 | **サポートされていません**（編集ボタンなし） |
| 複雑なSVG | 一部のエフェクト（フィルター、スクリプト）が完全に表示されない場合があります |
| ローカルインデックス | デバイスはSVGをMediaStoreに登録する必要があります。システムが一度もインデックスを作成していない場合、Downloadのみにあるファイルは欠落している可能性があります |

#### その他のフォーマットに関する注記

| フォーマット | 表示 | 編集 | ノート |
|------|------|------|------|
| `.gif` | ✓ | ✗ | 静止画のみ；アニメーションなし |
| `.heic` / `.heif` | ✓ | ✗ | デバイスのデコードによる
| `.bmp` | ✓ | ✗ | 閲覧可能、編集不可 |

#### ビデオフォーマット

アプリは、**ローカルギャラリー**および**ホームアルバム**の両方でこれらのビデオ拡張子を認識します。再生の詳細は§5.7に記載されています。

`.3gp`, `.avi`, `.flv`, `.m2ts`, `.m4v`, `.mkv`, `.mov`, `.mpeg`, `.mpg`, `.mp4`, `.qt`, `.rm`, `.rmvb`, `.swf`, `.ts`, `.vob`, `.webm`, `.wmv`

> 設定 → **メディアリストフィルター** には、これらの動画拡張子も参照用として一覧表示されています。

### 5.7 ビデオ再生

**ローカル**および**ホームアルバム**のビデオはアプリでストリーミングできます（ホームファイルは最初に完全にダウンロードする必要はありません）。スライドショーは§5.8です。

#### 認識されたビデオ拡張子

`.3gp`, `.avi`, `.flv`, `.m2ts`, `.m4v`, `.mkv`, `.mov`, `.mpeg`, `.mpg`, `.mp4`, `.qt`, `.rm`, `.rmvb`, `.swf`, `.ts`, `.vob`, `.webm`, `.wmv`

リストに載っているということは、アプリが**ファイルをビデオとして開く**ことを意味します。デコードを保証するものではなく、それは依然として内部のコーデックとデバイスに依存します。

#### 電話で

| トピック | ノート |
|------|------|
| エンジン | **Local** は **ExoPlayer (Media3)** を好む；一部のコンテナ／コーデックは **libVLC** を使用。**Home (SMB)** の一般的な `.mp4` / `.mkv` / `.webm` / `.m4v` / `.3gp` は Media3 を使用；`.mov` / `.ts` / `.avi` および類似のものは依然として **libVLC** を使用（コントロールバーの見た目は同じ）」
| よくある Media3 | ローカルおよびホーム `.mp4`、`.mkv`、`.webm`、および類似; 再生前に短いコーデックプローブが実行される場合があります |
| よく使われる libVLC | `.mov`、`.qt`、`.avi`、`.wmv`、`.flv`、`.mpg`、`.vob`、`.rm` / `.rmvb`、`.swf`、`.ts`、`.m2ts`、またはハードウェアでデコードしにくいコーデック |
| どちらも機能しない場合 | 別のシステムアプリで開く |
| ローカルスワイプ | ローカルギャラリー／タイムラインから、リスト内の前の動画／次の動画に左スワイプ／右スワイプ (Media3 パス) |
| クリップの終了 | コントロールバーに **リプレイ** が表示される |
| ネットワークバッファ | ホームストリームは最初の再生時にバッファリングする場合があります。一部のフォーマットや同じフォルダ内の字幕ファイルは、より安定したストリーム経路を使用する場合があります |
| 字幕 / レジューム / ブックマーク / ピクチャーインピクチャー | 同じフォルダ `.srt` / `.vtt` はプレーヤーで選択可能。レジューム / ブックマークは **ブックマーク** の下にある。**ピクチャーインピクチャーは主にまだローカルの Media3 を使用** |

> **コーデックの制限:** 同じ拡張子でも異なるコーデックを隠すことがあります。両方のエンジンが失敗した場合、音声のみ、黒いフレーム、または再生不可になることがあります。

#### 形式の概要

| 拡張子 | 認識済み | 電話再生 |
|--------|------|------|
| `.mp4` | ✓ | 普通は演奏する |
| `.mkv` | ✓ | 普通は演奏する |
| `.mov`、`.qt`、`.m4v`、`.3gp` | ✓ | コーデックによる（多くの場合は互換性エンジン） |
| `.avi`、`.wmv`、`.flv`、`.webm`、`.ts`、`.m2ts`、`.mpg`、`.mpeg`、`.vob`、`.rm`、`.rmvb`、`.swf` | ✓ | コーデックに依存 (libVLC) |

#### Cast ビデオをテレビに

ローカルまたはホームアルバムから**ビデオ**をCastすることは**Basic**です（フォトキャストは§5.9を参照）。電話とテレビは同じLANに接続されている必要があります。一部の拡張機能（例えば`.avi`、`.wmv`）はCastボタンを隠します。

| コーデック | キャスト方法 |
|------|----------|
| **H.264 / AVC**（一般的な携帯電話MP4、縦向き含む） | **直接キャスト**；ビットレートや縦向きのためだけにトランスコードはなし |
| **HEVC / H.265** およびその他のテレビ非対応コーデック | アプリはトランスコードします（「ビデオをキャストする準備中…」と表示されることがあります） |

「**この動画のビットレートが高すぎます**」は、**トランスコードが必要だが、ホームファイルがダウンロードされていない／トランスコードキャッシュが存在しない**場合にのみ表示されます。その理由でH.264がブロックされていないことを確認してください。電話で再生し、ローカルファイルまたはキャッシュが存在する状態で再度キャストを試みてください。

### 5.8 スライドショー

ローカルまたはホームギャラリーの**写真**を**電話画面**で自動再生します。BasicおよびProで利用可能です。**Pro**はスライドショーをテレビに送信することもできます（§5.9参照）。ビデオにはビデオプレーヤーを使用してください（§5.7）。

#### 始め方

| 方法 | 注意 |
|------|------|
| **複数選択から** | 長押しして選択 → 1枚以上の**写真**を選ぶ → **スライドショー**をタップ。全画面表示は**すぐに始まります** |
| **ビューアから** | 写真を開き、次に **スライドショーを開始** / **スライドショーを一時停止** (スライドショーアイコン) をタップします |

> 選択に**写真が含まれていない**場合（例えば、動画のみ）、「少なくとも1枚の写真を選択してください」と表示され、スライドショーは開始されません。

#### 再生

| トピック | ノート |
|------|------|
| 間隔 | 写真ごとに **約5秒** |
| ループ | 最後の写真の後、再生は最初に戻ります |
| 画面表示 | 左下にインデックス（`3 / 12`）を表示；右下にカウントダウンを表示；一時停止中は一時停止アイコンが表示される |
| 範囲 | マルチセレクトからは **選択された写真のみ**（選択順）；ビューアからは、現在の閲覧リストの写真 |

#### プレイ中のジェスチャー

| アクション | ノート |
|------|------|
| **タップ** | 一時停止 |
| **左にスワイプ** | 次の写真（カウントダウンがリセットされます） |
| **右にスワイプ** | 前の写真（カウントダウンがリセットされます） |

#### 一時停止後

| アクション | 注意 |
|------|------|
| **写真をタップ** | 再開（カウントダウンが再スタート） |
| **つまむ / ドラッグ** | 詳細表示のためにズーム（ピンチズームは一時停止中のみ有効） |
| ツールバー **スライドショーを一時停止** | スライドショーモードを終了して通常の表示に戻る |

システムバック：スライドショーが実行中の場合、最初の押下で終了し、もう一度押すとビューアを終了します（現在の画面によります）。

### 5.9 Cast の写真をテレビに

**Pro** はローカルまたはホームアルバムの写真を Google Cast TV / Chromecast にキャストできます。Basic は写真ビューアに Cast アイコンを表示し続けます；それをタップするとアップグレードが促されます。

| トピック | ノート |
|------|------|
| エントリー | 写真ビューアのタイトルバーの **Cast** アイコン |
| 接続後 | テレビに現在の写真が表示され、スマートフォンでピンチ、パン、スライドショーをするとテレビと同期します |
| スライドショー | 携帯電話で開始/一時停止すると、テレビも切り替わります |
| 視聴者を離れる | テレビの写真撮影が終了する |
| ビデオ | ローカル / 家庭用**ビデオ**Castの残りBasic（§5.7参照）、フォトキャストとは別 |

電話とテレビは同じLANに接続されている必要があります。初回はローカルネットワークの許可を与えてください。

---

## 6. 写真編集者

ビューアで **編集** をタップします（**`.jpg`、`.jpeg`、`.png`、`.webp` のみこのボタンが表示されます**、§5.6 を参照）。

編集は**下書き**です。層は二つあります：

1. ツール内で、下（縦向き）または右（横向き）を調整します；作業プレビューにそのステップを適用するには **✓** をタップし、ステップを破棄してツールピッカーに戻るには **X** をタップします。
2. ツールピッカーに戻ると、上部の **保存** はすべての適用されたステップをファイルに書き込みます。

> **ランドスケープ:** ポートレート / 調整 / トリミングに入ると、プレビュー用スペースのために上部のタイトルバーが隠れます；システムの「戻る」またはツールバーの **X** を押すと、そのツールを終了します。

### 6.1 ツール

3つのツールが下部（縦向き）または右側（横向き）に表示されます：

| ツール | 注意 |
|------|------|
| ポートレート | 顔の滑らかさと局所的なトーン（明るさ / コントラスト / 彩度） |
| 調整 | 明るさ、コントラスト、彩度、色温度 |
| トリミング | アスペクト比でトリミング、傾き、回転 / 反転 |

### 6.2 作物

ダークパネル；アイコン行からアスペクトを選択してください：

| 項目 | 注意事項 |
|------|------|
| 自由 | 角や辺を自由にドラッグ |
| 元の | 現在のアスペクト比を固定 |
| 3:2、4:3、16:9 | 風景固定比率 |
| 2:3、3:4、9:16 | 縦向き固定比率 |
| 1:1 | 正方形 |

- **傾き:** −15°から+15°、スケールをドラッグ；**リセット**で0に戻ります
- **オート:** 推奨される傾きは、オートをタップしたときのみ計算されます（クロップに入るときではありません）
- **反転 / 回転:** 下部の確認行の中央にあります；作業中の画像にすぐ適用されます（このトリミングステップを確定するには、後で✓が必要です）
- クロップボックスをドラッグして移動します；角をドラッグして拡大縮小します。三分割法のガイドはドラッグ中のみ表示されます
- ステップを適用するには **✓** をタップしてください

### 6.3 調整

ダークパネル；確認行の中央は4つのアイテムを切り替えます：

| 項目 | 注記 |
|------|------|
| 明るさ / コントラスト / 彩度 / 色温度 | −100から+100、中央は0；黄色いポインターが現在の値を示します |

- 右側の**リセット**は現在の項目をリセットします
- 回転/反転には**切り抜き**を使用してください
- ステップを適用するには **✓** をタップしてください

### 6.4 ポートレート

ダークパネル; ツールに入力すると**顔を検出**します（「再検出」も可能です）。確認行は切り替わります: 滑らかさ、明るさ、コントラスト、彩度。

| コントロール | ノート |
|------|------|
| スムーズ | 強さ 0–100; 顔がある場合は、顔の大きさに基づいた動的な提案（約20%–70%） |
| 明るさ / コントラスト / 彩度 | 顔のみに適用（または手動のボックス）、約±50% |

**顔が見つからない場合:**

- 滑らかさの強さは**0%**で、提案は表示されません
- 円形の領域が表示されます: **指1本でドラッグ**して移動し、**スライダー**でサイズを変更してから、スムーズに持ち上げます

**顔が見つかったとき:**

- 推奨スムーズは、フレームの最大の顔の割合を使用します（クローズアップは高め、グループ／遠景は低め）および「推奨 N%」を表示します
- 顔が輪郭で描かれている

ステップを適用するには **✓** をタップしてください。

### 6.5 保存とキャンセル

| ボタン | 場所 | 注意事項 |
|------|------|------|
| **X** | 工具の底 | この工具のステップを破棄し、ピッカーに戻る |
| **✓** | ツールの下部 | ツールの結果を作業用プレビューに統合する（まだ書き込まれていません） |
| **保存** | ツールピッカーの上部 | 適用されたすべての編集内容をファイル（ローカルまたはホームアルバム）に書き込む |
| 戻る | システム / トップに戻る | 保存されていない変更を破棄するかどうかを尋ねます |

> エディタープレビュー（ツールピッカー、ポートレート、調整）は**ピンチズーム**と**ダブルタップズーム**（約1×〜3×）をサポートし、その後パンが可能です。**クロップ**ではプレビューはズームされないため、ジェスチャーはクロップボックスにとどまります。

---

## 7. ブックマーク

### 7.1 ブックマークを追加する

ローカルまたは自宅のアルバムで、ツールバーの**ブックマーク**アイコンをタップします。「ブックマーク済み」というヒントが画面下部に表示されます。

ビデオを再生している間に、**星印**をタップして**ビデオブックマーク**（再生位置を含む）を追加します。

### 7.2 ブックマークを使用する

1. 下部バーの**ブックマーク**をタップします（そのタブをもう一度タップするとパネルを切り替えられます）
2. パネルで開くことができます:
- **フォルダブックマーク:** そのフォルダにジャンプ
- **ビデオブックマーク:** プレイヤーを開き、再開を試みてください

### 7.3 ブックマークを削除する

ブックマークパネルの項目を**長押し** → 確認（フォルダと動画のブックマークも同じ操作です）。

---

## 8. ホームアルバムにアップロード

1. ローカルギャラリーで**複数選択**を入力し、写真や動画を選択します
2. 下の **アップロード** アイコンをタップしてください
3. **ホームアルバム**（名前のみ）とオプションのサブフォルダを選択します（プライベートバックアップゾーンは選択できません）
4. 確認

**Proグレス**

- 進行状況（`N / M`）、ファイル名、およびバイトバーを表示します
- **停止** は残りのファイルをキャンセルします; 完了したファイルは保持されます; 結果は成功 / 部分成功 / キャンセル済みを区別します

**同じ名前**

- もし宛先にすでに同じ名前が存在する場合、アップロードは`name (1).ext`、`name (2).ext`、…と**名前が変更され**、**上書きされません**

**アップロードに成功した後、ローカルを削除する**

- すべてのアイテムが正常にアップロードされ、かつごみ箱が有効になっている場合にのみ、アプリは「ローカルのコピーをゴミ箱に移動しますか」と尋ねます
- **ゴミ箱に移動** または **デバイスに保持** を選択します。ゴミ箱オフの場合は成功メッセージのみが表示されます
- アルバムのバックアップ**では**これを提供していません；ファイルがすでにアルバムバックアップにある場合、ゴミ箱に移動した後でもリモートバックアップのコピーは保持されます

---

## 9. 設定

「**その他 → 設定**」を開きます。

### 9.1 ホスト接続

- 保存された**SMBホスト**を表示、編集、または削除
- ホスト接続の管理からホストを追加する（LANスキャンまたは手動アドレス）
- 接続メモはオンラインの **WebDAV / Tailscale** 遠隔セットアップページを開きます。同じページで **IPv6 を有効化** を切り替えます（デフォルトではオフ、§4.1 / §4.4 を参照）。
- 設定のホーム画面では **ホストSMBを追加** は **繰り返されません**。管理ページを使用してください

### 9.2 ホームアルバム（設定ブロック）

- 設定されているホームアルバムの数を表示します
- **フォトキャスト（Pro）：** ローカル／家庭の写真をテレビにキャストします。Basic スイッチをタップするとアップグレードを促されます；Pro は解除されています（ビューアーの Cast ボタンを使用）。自宅の **ビデオ** ストリーミングは Basic で、設定のスイッチはもう必要ありません
- **ポートレート H.264 直接キャスト:** ビデオCast互換オプション（Basic）；テレビの映像が回転している場合のみオフにしてください
- **ホーム**タブからアルバムを追加/削除する（ピッカー / 管理画面）

### 9.3 ローカルギャラリーフォルダー

- **フォルダーを含める:** 表示するルート（デフォルト `DCIM`、`Pictures`）
- **フォルダーを除外:** 特定のサブフォルダーをスキップ

**Basic 対 Pro（並行フォルダー）**

| エディション | 含めることができるパス | SAF 権限 |
|------|------|------|
| **Basic** | MediaStore ルート: `DCIM`、`Pictures`（動画も `Movies`）およびそのサブフォルダ; **さらに** 並列フォルダ | **フォルダの選択と許可（読み書き）** が必要 |
| **Pro** | Basic と同じ | Basic と同じ |

設定でBasicおよびProは`Documents`のような並行パスを許可できます。その権限がない場合、復元は`Pictures/…`にマッピングされることがあります（§9.7参照）。

**注意（Androidプラットフォームの制限）：** Android 11から、システムはフォルダピッカーで `Download` ルート全体の認可を **許可しません**—「このフォルダを使用」はグレーのままです。これはアプリのバグではありません。`Documents`（または同様のもの）を認可するか、Download の下に **サブフォルダー** を作成してそのサブフォルダーを認可してください。

**その二つの助成金を混同しないでください**

| 助成金 | 使用用途 |
|------|------|
| **フォルダを選択して認証 (SAF)** | 並列フォルダ自体の読み書き（復元、コピー / 移動 **に**） |
| **メディア管理（設定 → ローカルギャラリー）** | `DCIM` / `Pictures` のような標準アルバム内のオリジナルを削除または変更できます。カメラフォルダーから並列フォルダーへ移動すると、元のファイルを削除するかどうか尋ねられる場合があります。メディア管理にアクセスした後は、そのプロンプトは通常繰り返されません |

### 9.4 ディスプレイ

| 設定 | 注意 |
|------|------|
| ポートレートグリッドの列 | ポートレートの1行あたりの写真 |
| 横向きグリッドの列 | 横向きでの1行あたりの写真数 |
| フォルダ名のサイズ | ルートでのフォルダタイトルのサイズ |
| ビューアーアクション行 | 自動折りたたみ / 常に表示 |

### 9.5 キャッシュ

キャッシュサイズを確認し、**キャッシュをクリア**（ネットワークのサムネイル、画像ディスクキャッシュ、ネットワーク動画ストリームキャッシュ、メモリキャッシュ）。オリジナルはそのままです。次回のホームアルバム閲覧時には、キャッシュを再度ダウンロードまたは再構築する必要があります。

### 9.6 メディアリストフィルター

ローカルおよびホームアルバムリストでフォルダー名、ファイル名、または拡張子を非表示にします。組み込みの写真/ビデオ拡張子ルールは読み取り専用の参照です。

> 設定は次のようにグループ化されています: ローカルギャラリー、メディアフィルター、**ホスト接続**、ホームアルバム（ネットワークビデオストリーミングを含む）、アルバムのバックアップ、キャッシュ、**問題の報告 / 診断のエクスポート**、など。

### 9.7 アルバムのバックアップ

アルバムのバックアップは、ローカルギャラリーからホームストレージのプライベートゾーン（`.HomeGallery/backup/{device}/`）への**一方向のアップロード**で、電話が故障した後に復元できるようにします。

各デバイスは、それぞれのフォルダに`device_info.json`を保持します（モデル、任意の表示名、最後のバックアップ時間）。複数の人が同じバックアップアンカーを共有する場合は、アルバムバックアップ設定で**ローカルバックアップの識別 → 表示名**を設定してください。「既存のバックアップをリンク」リストには、`android_xxxx`だけでなく、その名前／モデルが表示されます。

| トピック | ノート |
|------|------|
| **方向** | **電話 → 家のストレージ**（一方通行） |
| **Basic / Pro** | **Basic:** 手動バックアップ、フォルダの除外、古い電話の復元 / 接続。 **Pro:** Wi‑Fiでの**自動**バックアップ（オプション） |
| **自動バックアップ** | **Proのみ。** Basic は手動でフォルダをバックアップおよび除外でき、バックグラウンド同期はありません。有効時（かつ「Wi‑Fiのみ」が満たされている場合）、自宅のWi‑Fi に接続すると **スキャン/アップロードをすぐに予定** します；アプリはまた約6時間ごとおよびギャラリーが変更されたときにも確認します。バッテリーセーバーや、まだ準備ができていないNAS はこれを遅延させる可能性があります—ベストエフォートであり、二次的なレベルではありません |
| **フォルダを除外** | **Basic / Pro:** ローカルのサブパスをスキップ（例えば `DCIM/Screenshots`）；すでにインデックス済みの項目はキューに残す |
| **ごみ箱に移動** | ホームバックアップは**削除されません**；また、「デバイスで見つからない」状態にも**なりません**（ごみ箱からローカルファイルを復元します） |
| **このアプリでの完全削除** | **“デバイス上で見つからない”ではない**; 次のバックアップで一致する`.HomeGallery`ファイルを削除し、トラッキングを解除する |
| **アプリの外に出た** | “デバイスに存在しない” と表示されます。ホームのコピーは手動での復元またはクリーンアップのために保持されます。**保留中のバックアップを確認** が NAS に接続した後、バックアップファイルがすでに存在しない項目はリストから削除されます（復元不可能なゴーストはありません） |
| **復元** | **バックアップの表示 / 復元** で、アイテムを **手動で** 選択し、ローカルに書き戻します |
| **未提供** | **双方向同期** — NASでバックアップファイルを直接変更/削除しても、電話のギャラリーは**更新されません** |

別の共有に**切り替える**、**ホストを変更する**、または**バックアップアンカーフォルダーを変更する**と、ローカルトラッキングは最初から始まります：同期済み／失敗／バックアップのみのカウントはクリアされます。**保留中のバックアップを確認**をタップして再度スキャンしてください（新しい保存先に既にこのデバイスのバックアップがある場合、アイテムは再アップロードせずに同期済みとして揃うことがあります）。

Basic「パブリックフォルダにアップロードする」とは異なり、バックアップゾーンはアプリ管理で、デフォルトでは通常のネットワーク閲覧から隠されており、ローカルコピーもデフォルトで保持します。

**バックアップゾーンを閲覧（大きなフォルダグリッド）**

1. **もっと → アルバムのバックアップ** → **バックアップゾーンを閲覧**
2. ホームアルバムフォルダモード（大きなグリッド）でプライベートバックアップルートを開き、必要に応じてフォルダを掘り下げる
3. **バックアップ専用** アイテムは赤い枠で表示されます。長押しして選択し、バックアップゾーンから削除（および追跡解除）できます
4. バックアップディスク上に存在するが、**ローカルインデックス行がない**ファイルは、**未追跡**とラベル付けされます（手動でコピーされたものや、インデックスリセット後のものが多いです）。バックアップ専用として表示される前に、スキャンまたはリモートインデックスを実行してください。
5. リスト形式の復元 / フィルターは、以下の **バックアップ / 復元を表示** をまだ使用します

**復元 (R1.1)**

1. **その他 → アルバムのバックアップ**（または設定のアルバムのバックアップ）→ **バックアップ / 復元の表示**
2. デフォルトフィルター **デバイス上に存在しない**（以前にバックアップ済みで、ローカルでは消滅；ごみ箱内のアイテムではなく、このアプリからの永久削除でもない）
3. **すべての不足を復元** をタップするか、複数選択して **選択を復元** → 確認（ファイル数、サイズ、**パスマッピング数**）→ 復元
（古い電話をリンクした後のアイテムは通常「バックアップのみ」です。デバイスにないフィルターも保留中のファイルを表示します）
4. 復元されたファイルがローカルギャラリーに表示されます

**並列フォルダーの復元パス（重要）**

バックアップに `Documents/…`: のような並列パスが含まれている場合

| 条件 | 復元が書き込む場所 |
|------|------|
| その並列パスに対して **SAF** なし | **`Pictures/{original relative path}`** にマッピング（例：`Documents/Office Lens/a.jpg` → `Pictures/Documents/Office Lens/a.jpg`; 動画は`Movies/…`の下にマッピングされる場合があります） |
| その並列フォルダーで **選択して認証** | **元の相対パス** に書き戻す（Picturesにはマッピングされない） |

Basic と Pro は両方とも並列フォルダを認可できます（設定 → ローカルギャラリーフォルダを参照）。

確認ダイアログにパスマッピングが表示される場合、一部のファイルは`Pictures`の下に配置されます。デバイス間復元（R2）も同じルールを使用します。

**新しい電話 (R2)**（既存のバックアップをリンク / ライブラリを完全復元; **Basic と Pro**）

1. **その他 → アルバムのバックアップ** → **バックアップを表示 / 復元**
2. **既存のバックアップをリンク** をタップ → 古い電話の `backup/{deviceId}/` を NAS で選択（**復元元のみ**）
3. インデックス作成後、この電話にないファイルには **バックアップのみ** フィルターを使用します
4. 複数選択または **すべての欠落を復元**（同じ並列フォルダルール: SAF がない場合、**`Pictures/…`** に移動）
5. この電話からの後のバックアップは依然として**自分自身の**フォルダに書き込まれます。古い電話は別々にバックアップを続けることができます

**アプリのストレージをクリアした後（同じ電話）**

1. バックアップの後、まず **設定をエクスポート** します（これによりバックアップ`deviceId`も記憶されます）
2. アプリのストレージをクリア（または再インストール）→ 再度権限を付与 → **できるだけ早く設定を復元**（最初にアルバムのバックアップを有効にしないこと、新しいデバイスIDが作成される可能性があります）
3. アルバムのバックアップを開く → **保留中のバックアップを確認**: アプリはホームバックアップゾーンに接続し、ローカルファイルでNASとまだ一致するものを同期済みとしてマークします—**完全再アップロードは不要**
4. すでに異なる識別情報を持つ**別の電話**で設定を復元する場合：書き込み識別情報はそのまま残ります；インポートされた古い `deviceId` はリンクされた復元元として扱われます（インデックス化／復元するには「既存のバックアップをリンク」を使用してください）
5. エクスポートが古い場合（`deviceId`がない場合）、バックアップの後に再度エクスポートするか、「既存のバックアップをリンク」して古いフォルダを選択してください

### 9.8 設定のエクスポートと復元

| トピック | ノート |
|------|------|
| **エクスポート設定** | ローカルギャラリー、SMBホスト、自宅アルバム、ブックマーク、およびバックアップ設定をDownload/Home Galleryにエクスポートします |
| **認証情報** | ファイルにはSMB個のパスワードが**エンコード**形式で含まれています（プレーンテキストではなく、強力な暗号化でもありません）；非公開にしてください—公共のクラウドにアップロードしないでください |
| **設定を復元** | 最近のエクスポートまたはJSONファイルを選択します；これにより現在の設定と資格情報が**上書き**されます |

> リファクタリング前の古い（`SambaProfile`）テストビルドを使用していた場合は、**アプリのデータを消去するか再インストール**し、再度ホストとホームアルバムを設定してください。古い形式の設定ファイルはインポートしないでください。

### 9.9 問題の報告と診断

| トピック | ノート |
|------|------|
| **問題を報告する** | メールを開き、オプションでバージョンと機能フラグの概要を含める |
| **診断のエクスポート** | 写真コンテンツやSMBパスワード**なし**で、`diagnostics-*.zip`（デバイス情報、最近のエラー、`last_crash.txt`など）を作成します |
| **クラッシュ後** | ZIPに最新の致命的クラッシュスタックトレースが含まれるように、エクスポートする前に**アプリを再度開いてください** |

---

## 10. よくある質問

### Q1: ローカルの写真が見つかりませんか？

- **写真とビデオ**へのアクセスが許可されていることを確認する
- 設定 → ローカルギャラリーフォルダー：フォルダーが **フォルダーに含める** にあることを確認してください

### Q2: ホームストレージに接続できませんか？

- 携帯電話とNAS/ PCは同じ家庭用Wi‑Fi/ LANに接続されている必要があります
- 設定 → **ホスト接続**：アドレス、アカウント、パスワードを確認
- NAS / PC（ポート445）でSMBが有効になっていることを確認してください
- スキャンで何も見つからない場合：Wi‑Fi をオンにし、ストレージと同じLANに接続したまま、再度スキャンしてください
- **離れている間は接続できません:**
- NAS: リモートアクセスは **WebDAV** である必要があり、正しい DDNS / ポート → [WebDAV guide](./setup-home-storage/webdav.md)
- Windows / なし WebDAV: [Tailscale guide](./setup-home-storage/tailscale.md) を参照
- その他の場合: [Troubleshooting §E](./setup-home-storage/troubleshooting.md)

### Q3: ローカルの写真を削除または編集する際にシステムの権限確認が表示されますか？

Android 11以上では、他のアプリによって作成された一部のメディアは変更や削除するために追加の権限が必要です。システムダイアログで許可してください。

### Q4: ポートレート／スムーズは弱く見えますか？

- 顔が検出されたことを確認するか、円をドラッグしてスライダーでサイズを変更してください
- **滑らかさ**を上げる
- 適用 (✓) してから保存

### Q5: 編集後に品質は低下しますか？

ポートレート**プレビュー**は速度のために低解像度を使用します；**保存**は元の解像度で処理します。まだ強すぎる場合は、スムーズを下げてください。

### Q6: ホームアルバムへのコピー／アップロードは遅いですか？

速度はネットワーク帯域幅とファイルサイズに依存します。安定したWi‑Fi接続を使用してください。

### Q7: なぜSVGは表示できるのに編集できないのですか？

SVGはベクターです。このアプリはベクターを編集してSVGとして保存することはできず、ラスター化されたビットマップのみを**表示**できます。そのため、編集は非表示になっています。§5.6を参照してください。

### Q8: GIF / HEIC に編集ボタンがないのはなぜですか？

これらの形式は表示できますが、編集された保存では元の形式（GIFアニメーション、HEICエンコード）を保持できません。損傷を避けるため、編集は `.jpg`、`.jpeg`、`.png`、`.webp` のみに対応しています。

### Q9: ローカルギャラリーにSVGファイルが見つかりませんか？

Androidが**MediaStore**をインデックスしたことを確認してください（Download秒は一般的なケースです）。システムがSVGをメディアとして登録していない場合、このアプリはそれを一覧表示できません。

### Q10: 動画が表示されているが再生できませんか？

- 拡張が§5.7にあることを確認してください
- 電話で: アプリは組み込みエンジン（Media3 または互換性エンジン）を試します; それが失敗した場合、**別のアプリ**で開きます。同じファイルを別のプレーヤーでテストすることもできます
- ネットワークビデオ：ネットワークを確認してください。必要に応じて、設定でキャッシュをクリアして再試行してください

---

## 11. 権限

| 許可 | 理由 |
|------|------|
| 写真と動画 (READ_MEDIA_IMAGES / READ_MEDIA_VIDEO) | ローカルの写真と動画を読み取る |
| インターネット (INTERNET) | 自宅SMBストレージに接続 |
| ネットワーク状態 (ACCESS_NETWORK_STATE) | 接続を検出 |
| Wi‑Fi 状態 (ACCESS_WIFI_STATE) | LAN検出 |

---

## 12. Basic と Pro

ホームギャラリー は **Basic** と **Pro** を提供します。Basic は **広告なし** で、すでにフルの写真・ビデオ管理（ローカルおよびホームアルバム、タイムライン、ホームビデオストリーミング、ごみ箱、並列フォルダ、手動アルバムバックアップとフォルダ除外、古い電話の復元/リンク、その他多数）を含んでいます。追加のスペースはクラウドの割り当てではなくホームストレージから提供されます。**Pro** は **任意の一度きりの購入** で、さらに **自動アルバムバックアップ**（Wi‑Fi でのバックグラウンドアップロード）と **写真のテレビへのキャスト** をアンロックします。

| エディション | 役割 |
|------|------|
| **Basic** | 完全なローカルおよびホームメディア管理；手動バックアップ、フォルダの除外、および復元 |
| **Pro** | Basic のすべて + **自動バックアップ**（バックグラウンド Wi‑Fi 同期）+ **フォトキャスト**（テレビ） |

> Prouction ビルドでは、アプリ内購入で Pro を解除できます；デバッグビルドでは、テストのために設定で関連する機能を切り替えることができます。

### 12.1 Basic — 無料

| 特徴 | 注記 |
|------|------|
| ローカルギャラリー | 閲覧、タイムライン、検索、並べ替え；並列フォルダー（SAF） |
| ホームアルバム | ホスト、写真、**タイムライン**、ブックマーク；**ホームビデオ**ストリーム |
| 写真管理 | コピー、移動、名前変更、削除、共有 |
| 写真編集 | トリミング、調整、ポートレート、その他 |
| アップロード | ローカルの写真・動画を**ホームアルバム**フォルダにアップロード |
| **アルバムバックアップ** | **手動**の片方向バックアップ; **フォルダを除外**; **復元** / 古い電話にリンク; **自動**バックグラウンド同期なし |
| **ごみ箱** | 設定で有効にする; 削除したものはごみ箱に移動され、復元可能 |
| ローカル / ホームビデオ | 再生およびCast（機器依存） |
| スライドショー | 携帯電話で写真を自動再生する（§5.8参照） |
| 写真キャスト（テレビ） | **Proが必要**（§5.9参照） |
| 設定 | **もっと見る → 設定**: ホスト、ギャラリー範囲、キャッシュ、その他 |

### 12.2 Pro — 一回購入

Basicの上にあるすべてのものの上に：

| 特徴 | 注記 |
|------|------|
| **自動バックアップ** | Wi‑Fiでの**自動**一方向バックアップ（バックグラウンドスケジュール）；手動バックアップ、フォルダの除外、復元はBasicと同じまま |
| **フォトキャスト** | Cast ローカル／家庭の写真やスライドショーをテレビに表示（ピンチ、パン； §5.9参照） |

### 12.3 関連設定スイッチ

| どこ | 注釈 |
|------|------|
| 設定 → **ホームアルバム** → フォトキャスト | **Pro:** 写真 / スライドショーをテレビに。Basic スイッチをタップするとアップグレードの案内が表示されます |
| 設定 → **ホームアルバム** → ポートレート H.264 直接キャスト | ビデオ Cast 互換性 (Basic) |
| 設定 → **アルバムのバックアップ** | バックアップを有効にする、フォルダを除外する、**自動同期**（自動同期は⦰0⦰のみ） |
| 設定 → **ローカルギャラリー** → ごみ箱 | ごみ箱と削除デフォルト（BasicとPro）を有効にする |

Pro の購入/復元が成功した後、一度だけ「**あなたは Pro にいます**」というダイアログが表示されます。その後の起動（プロセスが終了した後も含む）では再度表示されません。

**ごみ箱のオプション**（設定 → ローカルギャラリー → ごみ箱のオプション）：デフォルトの削除は**ごみ箱に移動**または**完全に削除**; ごみ箱を空にする前に確認する; 有効期限のリマインダー（システムが約1日以内に完全に削除するアイテムの毎日の通知; Android 13+ は通知の許可が必要）。

- **デフォルト = ごみ箱に移動:** ローカルギャラリー / ビューアから削除してごみ箱に移動（復元可能）。
- **デフォルト = 永久に削除:** 確認後、ローカルギャラリー/ビューアーでの削除は実際に削除されるべきであり、**その削除のために**ゴミ箱に入るべきではありません。ゴミ箱には他の理由でアイテムが表示されることはまだあります:
1. **完全に成功したアップロード**の後、ローカルコピーをゴミ箱に移動することを選択しました（削除デフォルトとは独立しています）
2. **写真編集** は、上書きする前に編集前のバックアップをゴミ箱に保存します
3. **他のアプリ / システムギャラリー** がアイテムを共有の MediaStore ゴミ箱に移動しました
- ゴミ箱が**オフ**の場合、ローカルでの削除は常に永久的です。アップロードが成功しても、コピーをゴミ箱に移動するかどうかは尋ねられません。

### 12.4 アップグレードの方法

1. **さらに → 設定 → Pro にアップグレード**（または、自動バックアップ、フォトキャストなどを試すと表示されるアップグレードのプロンプト）
2. Google Play の一度きりの購入を完了する
3. 新しい電話や再インストール後は、設定で**購入を復元**を使用してください

### 12.5 Pro よくある質問

**Q: Basic は編集、削除、アップロードできますか？**
はい。Basicは閲覧専用アプリではなく、完全な写真管理アプリです。

**Q: ローカルまたはホームビデオにはProが必要ですか？**
いいえ。ローカルおよびホームビデオの再生はBasicです。Proは**自動**アルバムバックアップと**フォトキャスト**のロックを解除します。

**Q: 写真をテレビにキャストするにはProが必要ですか？**
はい。Basicは電話でスライドショーを再生できます；テレビキャストにはProが必要です。ビデオCastはBasicのままです。

**Q: ビデオキャストで「ビットレートが高すぎます」と表示されるのはなぜですか？**
トランスコードが必要だが、ローカルファイルやトランスコードキャッシュがまだ存在しない場合のみ。典型的な電話の**H.264**は直接キャストされ、ビットレートによってブロックされることはありません。HEVCや他のテレビ非対応コーデックは先に準備されます。§5.7を参照してください。

**Q: アップロードはバックアップとどう違いますか？**
アップロードとは、あなたがファイルと**ホームアルバム**フォルダ（BasicとPro）を選ぶことです。**ゴミ箱がオン**の場合、完全に成功したアップロードはローカルのコピーをゴミ箱に移動してスペースを節約できます。バックアップはローカルギャラリーを**一方向**でアプリ管理の**プライベートバックアップゾーン**に書き込み、デフォルトでローカルコピーを保持します；必要なときにアプリで復元します。

**Q: アルバムのバックアップはBasicとProでどのように異なりますか？**
Basic は **手動** バックアップ、**フォルダーの除外**、旧端末の **復元** / リンクをサポートします。**Pro** はさらに Wi‑Fi で **自動** バックアップをバックグラウンドで有効にすることができます。

**Q: バックアップは双方向ですか？**
いいえ。バックアップは **電話 → NAS** です。NAS 上のバックアップファイルを編集しても、電話には書き戻されません。復元はアプリでの意図的な操作です。

**Q: Office LensのファイルはDocumentsに保存されます。Basicはそれを見ることができますか？**
はい。Basic と Pro は、設定 → ローカルギャラリーで `Documents` のような並列フォルダーを **選択して承認** できます。

**Q: 復元する際、Documentsや他の並列フォルダーはどこに行きますか？**
**そのフォルダーに対してSAFなし**: **`Pictures/`以下のマップされたパス**（`Documents/…`には戻らない）。**その並列フォルダーが許可されている場合**: 元の相対パス。復元確認ダイアログには、何本のパスがマップされているかが表示されます。§9.7を参照してください。

**Q: Downloadを認証するときに「このフォルダーを使用」がグレー表示になるのはなぜですか？**
これは**Android 11+ システム制限**です。アプリはDownloadの**ルート**に対してツリー権限を取得できません。`Documents`を選択するか、Downloadの下にある**サブフォルダ**を認可してください。アプリは「読み取り専用でDownloadを含める」パスを提供していません（コピー／復元の書き込みと矛盾し、より扱いにくくなるためです）。

---

## 13. バージョン情報

「その他 → バージョン情報」（または「その他 → 設定 → バージョン情報」）で以下を見ることができます:

- バージョン（現在 **1.2.3**；アプリ内の **About** を確認してください）
- 製造日
- **ユーザーガイド**（このオンラインマニュアル: https://jasoncschang-stack.github.io/homegallery-privacy/help/）
- 問題を報告する
- 著作権

設定 → **サポート** を開くと、同じガイドが表示されます。

この公開ガイドは、アプリ **1.2.3** およびマニュアル **1.48** に対応しています。
