<!-- Source: English user manual 1.48 / App 1.2.3; machine-translated to Korean -->
# 홈 갤러리 사용자 매뉴얼

| 항목 | 값 |
|------|------|
| 매뉴얼 버전 | 1.48 |
| 앱 버전 | 1.2.3 |
| 작성일 | 2026/09/20 |

---

## 1. 소개

**홈 갤러리** (家相簿)은 **장치 내** 미디어와 **가정 저장소**(NAS / SMB를 통한 PC)를 위한 안드로이드 사진 및 비디오 관리 앱입니다. **원하는 공간을 선택하세요—추가 클라우드 용량을 구매할 필요가 없습니다.** 이미 가지고 있는 디스크를 사용하세요; 사진은 사용자가 제어하는 저장소에 남습니다.

- **로컬 갤러리**에서 사진과 동영상을 탐색하고 관리하세요
- **홈 앨범**을 NAS / PC의 공유 폴더에 연결하기 (각 카드에는 하나의 호스트 + 하나의 공유 + 하나의 폴더가 있음)
- 설정의 **호스트 연결**에서 SMB 자격 증명을 한 번 등록하세요 (한 번 스캔하고, 한 번 로그인하면 나중에 재사용 가능)
- 사진을 보고, 편집하고, 공유하고, 복사하고, 이동하고, 이름을 바꾸고, 삭제하세요
- **슬라이드쇼**는 선택된 사진(로컬 / 홈 앨범)을 자동 재생합니다; **Pro**는 슬라이드쇼를 TV로도 전송할 수 있습니다
- 자주 사용하는 폴더를 즐겨찾기
- 로컬 **타임라인** 탐색; **홈 앨범 타임라인**으로 날짜별 홈 미디어 탐색
- **가정용 비디오**는 휴대폰에서 스트리밍할 수 있습니다 (전체 파일을 먼저 다운로드할 필요 없음)
- **UI 언어:** 영어, 번체 중국어, 간체 중국어, 일본어, 한국어 (시스템 언어에 따름)

이 앱은 **클라우드 갤러리**를 호스팅하지 않습니다. SMB 비밀번호는 사용자의 기기에만 저장됩니다. **Basic**는 광고 없는 완전한 사진 관리 앱으로, 슬라이드쇼, 홈 비디오 스트리밍, 타임라인, 휴지통, 병렬 폴더, **수동** 앨범 백업, **백업 영역 탐색**, 그 외 기능들을 제공합니다. **Pro** (선택 사항, 일회성 구매)는 추가로 **자동** 앨범 백업과 **사진/슬라이드쇼 TV 전송** 기능을 잠금 해제합니다.

**요구 사항:** Android 11 (API 30) 이상.

---

## 2. 시작하기

### 2.1 앱 열기

앱이 **로컬 갤러리**에서 열립니다. 아직 사진 접근 권한이 허용되지 않았다면 **사진 및 동영상**을 허용하세요.

### 2.2 주요 내비게이션 (하단 바)

**하단 내비게이션 바**는 주요 영역을 전환합니다(**가로 모드**에서는 동일한 항목으로 측면 레일이 됩니다). 네 개의 탭이 있습니다:

| 탭 | 기능 |
|------|------|
| **로컬** | 기기 내 갤러리 (폴더 / 타임라인). **휴지통**이 활성화된 경우, 로컬 갤러리 제목 표시줄에서 열기 |
| **홈** | 마지막 홈 앨범을 엽니다; 다시 탭하면 **앨범 선택기**를 열거나 닫습니다 (앨범 전환, 홈 앨범 관리) |
| **북마크** | 북마크 패널 열기/닫기: 폴더 북마크 및 **동영상 북마크**; 탭하여 이동하거나 계속 재생 |
| **더보기** | 메뉴: **앨범 백업** (비활성화된 경우 설정 토글로 이동; 활성화된 경우 백업을 열고 보류 중/실패 배지가 표시될 수 있음), **설정**, **정보** |

> **사이드바**가 없습니다. 또한 '네트워크 저장소' 연결 목록도 없으며, 홈 파일은 항상 **홈** 탭에서 열립니다.
> **정보**(평가 / 공유 / 버전)는 **더보기 → 정보**에서 확인할 수 있으며; 설정 → 지원에서도 열 수 있습니다. Pro로 업그레이드하고 문제를 신고하는 것도 설정에 있습니다.

**홈/북마크 두 번째 탭:** 이미 해당 탭에 있는 경우, 같은 아이콘을 탭하면 현재 탐색 중인 내용을 벗어나지 않고 선택 패널이 토글됩니다.

멀티 선택 중이거나 뷰어/플레이어에서, 하단 바가 숨겨졌다가 탐색으로 돌아가면 다시 나타날 수 있습니다.

### 2.3 앱 종료

로컬 갤러리, 홈 앨범 또는 설정과 같은 주요 화면에서 처음 **뒤로** 버튼을 누르면 '다시 누르면 종료됩니다'가 표시됩니다. 2초 이내에 다시 누르면 앱이 종료됩니다.

---

## 3. 지역 갤러리

### 3.1 사진 탐색

- **루트**는 폴더(예: DCIM, Pictures)를 보여줍니다
- 폴더를 열어 사진 그리드를 확인하세요
- 사진을 **전체 화면**으로 보려면 탭하세요
- **휴지통**이 활성화된 경우: 제목 표시줄이 **휴지통**을 엽니다 (항목이 있을 때 카운트 배지가 나타납니다; Basic과 Pro은 설정에서 이를 활성화할 수 있습니다)

### 3.2 검색 및 정렬

- 상단 검색 필드를 사용하여 **파일 이름**으로 검색합니다; 오른쪽의 **X**는 텍스트가 있을 때 지우거나, 비어 있을 때 검색을 종료합니다
- 필드(이름, 수정 날짜, 캡처 날짜, 크기, 폴더) 및 오름차순/내림차순을 변경하려면 정렬을 탭하세요

### 3.3 다중 선택

1. 사진/동영상을 **길게 누르면** 선택 모드로 들어갑니다; **폴더** 루트에서는 폴더를 길게 눌러 안의 모든 미디어를 선택할 수 있습니다
2. 다른 항목을 추가하려면 탭하세요 (폴더 모드에서는 폴더 전체를 선택하거나 해제하려면 폴더를 탭할 수도 있습니다)
3. **범위 선택:** 이미 선택 중인 상태에서, 다른 항목(또는 폴더)을 **길게 누르면** 현재 탐색 순서에서 그 사이의 모든 항목(포함)을 선택할 수 있습니다; 기준점은 선택에 들어간 마지막 탭이나 길게 누른 것입니다
4. 하단 도구 모음에는 다음이 표시됩니다: **슬라이드쇼**, **복사**, **이동**, **삭제**, **공유**, **홈 앨범에 업로드**
5. 폴더 모드에서 전체 폴더의 미디어를 삭제한 후, 경로에 파일이 남아 있지 않으면 앱이 **빈 폴더를 제거하려고 시도**합니다 (`DCIM` / `Pictures`과 같은 시스템 루트는 삭제되지 않으며; 미디어가 아닌 파일이 남아 있는 폴더는 유지됩니다)

> **슬라이드쇼:** 다중 선택 후, 선택한 **사진**을 자동 재생하려면 **슬라이드쇼**를 탭하세요(비디오는 건너뜁니다). §5.8 참조.

### 3.4 즐겨찾기

갤러리 도구 모음에서 **북마크** 아이콘을 눌러 현재 폴더를 북마크하세요; 나중에 **북마크** 탭에서 열 수 있습니다.

### 3.5 탐색 모드

지역 갤러리 **root**에서 도구 모음의 보기를 전환하세요:

| 모드 | 보이는 것 |
|------|------|
| **폴더** | 폴더별 탐색(기본값); 루트는 먼저 폴더를 표시하고, 그 다음 사진/비디오 그리드를 표시 |
| **타임라인** | 모든 미디어를 **촬영 날짜** 기준 최신순으로, 하루 단위로 그리드에 그룹화 |

**타임라인:**

- 하단 **날짜 축:** 연도 / 월 / 일 단위; 확대/축소로 변경하거나 힌트에서 ‘연도 / 월 / 일’을 탭하세요
- 일 단위 세분화에서는 축에 미리보기 이미지가 표시됩니다. 레이블은 날짜(중국어 로케일 `M月d日`에서)와 연도입니다.
- 오른쪽에 **빠른 스크롤러**(콘텐츠가 충분할 때)가 빠르게 이동합니다
- 검색, 다중 선택, 뷰어 및 동영상 재생이 지원됩니다

**공유됨:**

- 즐겨찾기는 여전히 폴더 경로를 가리킵니다; 즐겨찾기를 열면 폴더 모드로 돌아갑니다

**설정 → 로컬 갤러리 → 그리드 표시**는 기본 탐색 모드(폴더 / 타임라인)와 기본 정렬을 설정하며, 앱은 마지막으로 사용한 모드도 기억합니다.

---

## 4. 홈 앨범과 SMB 호스트

> **PC / NAS에서 아직 폴더를 공유하지 않았나요?** 독립 실행형 설정 가이드부터 시작하세요:
> [Set up home storage](./setup-home-storage/) ([Windows 11](./setup-home-storage/windows-11.md) · [Synology](./setup-home-storage/synology.md) · [QNAP](./setup-home-storage/qnap.md); 연결할 수 없으면 [Troubleshooting](./setup-home-storage/troubleshooting.md) 참조; **원격 NAS** 참고 [WebDAV](./setup-home-storage/webdav.md); **원격 Windows / WebDAV 없음** 참조 [Tailscale](./setup-home-storage/tailscale.md)).

홈 파일은 두 개의 레이어를 사용합니다:

| 개념 | 의미 |
|------|------|
| **SMB 호스트** | 하나의 NAS / PC 및 자격 증명 (설정 → **호스트 연결**에 등록됨) |
| **홈 앨범** | 한 카드 = 해당 호스트의 **한** 개 공유 + 그 안의 한 폴더 경로 |

### 4.1 SMB 호스트 추가

1. **더보기 → 설정 → 호스트 연결 → 호스트 연결 관리**를 엽니다
2. **SMB 호스트 추가**를 탭하세요(또는 홈 앨범 흐름에서는 “새 SMB 호스트 찾기”를 탭하세요)
3. 대화 상자가 LAN을 스캔하고 **Wi‑Fi이 켜져 있으며 스토리지와 동일한 네트워크에 있는지 확인**하도록 요청합니다
4. 호스트 선택(이미 저장된 호스트는 흐리게 표시됨) → 자격 증명 입력; 호스트 이름이 사용 가능할 때 표시 이름으로 사용됨 → **연결 테스트** → **저장**
- **LAN IPv4:** 가정용 Wi‑Fi (예를 들어 `192.168.x.x`)
- **원격 접근** (선택 사항, §4.4 참조):
- **WebDAV:** 잠시 NAS 동안; **외부 호스트(DDNS / 공용 IP)** 와 **HTTPS 포트** 입력 (보통 `5006`)
- **Tailscale:** Windows용 또는 WebDAV를 사용할 수 없을 때; `100.x`를 입력하세요
- **없음:** 집 LAN 전용, 외부 도움 없음
5. 호스트 주소를 수동으로 입력할 수도 있습니다

**연결 메모(설정에서)**

- 홈 앨범은 **같은 집 Wi‑Fi (LAN)**용입니다; 집에서는 앱이 **SMB**를 사용합니다
- 집을 떠나 있을 때 모바일 데이터 사용:
- **NAS:** **WebDAV** (DDNS / 공인 IP) 선호; [WebDAV guide](./setup-home-storage/webdav.md) 참조
- **Windows PC** 또는 없음 WebDAV: **Tailscale** 또는 기존 LAN VPN 사용; [Tailscale guide](./setup-home-storage/tailscale.md) 참조
- UI는 '홈 LAN' 또는 '외출'을 표시할 수 있습니다; 외출 모드에서도 WebDAV, Tailscale 또는 올바르게 구성된 VPN이 필요합니다
- 호스트 연결 관리에서 **연결 메모** 카드가 온라인 **WebDAV / Tailscale** 설정 페이지를 엽니다
- **IPv6 활성화** (같은 페이지, 기본값 **끔**): 끄면, 검색 및 연결은 IPv4만 사용하고 IPv6 필드는 숨겨집니다; 저장된 IPv6는 유지됩니다. 켜면 IPv6를 스캔 / 사용할 수 있습니다. 자리 비움 시, WebDAV / Tailscale를 사용하세요 (§4.4 참조)

### 4.2 홈 앨범 추가

1. 하단 내비게이션 → **홈** (아직 앨범이 없으면 설정이 제공됩니다; 또는 **홈**을 다시 탭 → **홈 앨범 관리**)
2. 아직 앨범이 없다면, **홈 스토리지 연결**(또는 **추가** FAB)을 탭하세요
3. **호스트가 아직 없는 경우:** Add SMB 호스트 대화 상자가 열립니다; 저장 후 설정이 계속 진행됩니다
4. **이미 호스트가 존재하는 경우:**
- 한 호스트: **공유 선택**으로 바로 이동
- 여러 호스트: 호스트를 선택한 다음 공유를 선택
- 스캔이 필요할 때 **새 SMB 호스트 찾기** 가능
5. **앨범 폴더 선택:** 공유 폴더 내의 하위 폴더를 탐색합니다; 앨범 루트에서 어느 수준에서든 멈출 수 있습니다
6. **표시 이름(선택 사항)**은 폴더 이름으로 기본 설정 → **완료**를 탭합니다 (이렇게 하면 **하나의** 앨범이 생성됩니다)
7. **홈** 탭에서 열고; 앨범을 전환하려면 다시 **홈**을 탭하세요

> 설정은 더 이상 전체 호스트 검사를 다시 실행하지 않으며, 여러 공유를 다중 선택하여 한 번에 여러 앨범을 만들 수 없습니다.

### 4.3 찾아보고 관리하기

1. 하단 내비게이션 → **홈** → 앨범 열기 (또는 **홈**을 다시 탭하고 선택기에서 전환)
2. 해당 소스 폴더를 찾아보기(그리드 / 목록)
3. 제목 표시줄은 **폴더** / **타임라인**을 전환합니다. 타임라인은 폴더별로 날짜별 사진과 동영상을 보여줍니다 (**Basic**). 첫 번째 열 때는 인덱스를 생성하며, 이후 방문에서는 캐시를 선호합니다. 인덱스가 약 24시간 이상 오래된 경우, 연결 후 조용히 새로 고침되며, 수동으로 새로 고침할 수도 있습니다. 타임라인에서 현재 보기에 가까운 폴더를 새로 고치려면 **끌어당기기**로 새로 고침(부분 새로 고침)을 할 수 있으며 전체 라이브러리 재스캔은 필요 없습니다.
4. 하위 폴더 안에서 타임라인으로 전환하면 **해당 폴더의 범위만** 표시됩니다
5. 사진을 탭하여 보기; 도구 모음에서 **새로 고침**할 수 있습니다. 타임라인에서 **이름 변경** 후, 인덱스는 새 이름을 따릅니다; 이후의 **이동**은 새 경로를 사용합니다
6. 타임라인 다중 선택은 **시간을 맞추거나** (참조 사진과 일치) 또는 단일 사진의 **시간을 수정**할 수 있습니다 (날짜와 시간)
7. 홈 앨범 **관리** 화면에서 각 앨범은 **타임라인 스캔 깊이**(1–8 단계, 기본값 4; 앨범 루트는 0 단계)를 가집니다

**앨범 링크 제거**

- 홈 앨범 **관리** 화면에서: 카드를 길게 누르거나 ⋮ 을 탭 → **앨범 링크 제거**
- 이것은 앱에서의 링크만 제거하며, 저장 장치의 파일을 **삭제하지는 않습니다**

**다중 선택 이동 / 복사**

- 대상 선택기에서는 홈 앨범을 표시 이름으로만 나열합니다(두 번째 줄 경로 없음). 그런 다음 하위 폴더를 입력할 수 있습니다.

### 4.4 원격 접근(선택 사항)

집을 떠난 후 Wi‑Fi, 호스트 유형에 맞는 원격 방법 하나를 선택하세요:

| 호스트 유형 | 원격 접속 | 비고 |
|----------|----------|------|
| **NAS** (HTTPS WebDAV 활성화됨) | **WebDAV** | DDNS / 공용 IP + 포트; [webdav.md](./setup-home-storage/webdav.md) 참조 |
| **윈도우 PC** / 아니오 WebDAV | **Tailscale** | `100.x` 입력; 아래와 [tailscale.md](./setup-home-storage/tailscale.md) 참조 |
| 이미 OpenVPN 등을 통해 LAN에 연결됨 | **없음** (또는 LAN 주소 유지) | 보통 추가 WebDAV / Tailscale 없음 |

#### WebDAV (NAS에게 추천됨)

1. 홈 Wi‑Fi에서 먼저 호스트와 홈 앨범을 설정합니다 (§4.1–4.2)
2. NAS에서 **WebDAV HTTPS**를 활성화하고 **DDNS** 또는 공인 IP를 준비합니다
3. 호스트 편집 → **원격 액세스 = WebDAV** → 외부 호스트와 포트 입력 → **연결 테스트** → **저장**
4. (대부분의 가정용 NAS은 자체 서명 인증서를 사용합니다) **신뢰할 수 없는 HTTPS 인증서 허용**을 활성화할 수 있습니다: 앱은 먼저 **시스템 신뢰**를 시도합니다; 실패하면 해당 호스트에 대해 **인증서를 고정(TOFU)**합니다—첫 번째 성공적인 연결이 지문을 저장합니다. 나중에 NAS 인증서가 변경되어 연결이 실패하면, 이 옵션을 껐다가 다시 켜서 인증서를 다시 고정하십시오
5. 집에 없을 때 홈 앨범 열기 (확인을 위해 홈 Wi‑Fi 끄기)

전체 단계 및 보안 참고 사항: [setup-home-storage/webdav.md](./setup-home-storage/webdav.md).

#### Tailscale (윈도우 / WebDAV 없음)

휴대폰이 **`100.x`**에서 집 SMB에 도달할 수 있도록 **Tailscale**을 안전한 터널로 사용하세요.

**짧은 흐름:**

1. 홈 Wi‑Fi에서 먼저 호스트와 홈 앨범을 설정합니다 (§4.1–4.2)
2. **PC / NAS:** Tailscale 설치 → 로그인 → 연결됨 → **그 컴퓨터의 Tailscale IPv4 (100.x) 복사**
3. **전화:** 설치 Tailscale → **같은 계정** → 연결됨
4. 집 앨범을 외출 중에 열기 → 메시지가 표시되면 **Tailscale 열기** → PC `100.x` 복사 → 앱으로 돌아가기 → **붙여넣기** → **Tailscale IP 저장 후 재시도**

**일상:** 앨범이 필요할 때 Tailscale을 엽니다. 앱을 나가거나 홈 화면으로 돌아가면 Wi‑Fi, 앱이 전력을 절약하기 위해 Tailscale 연결을 끊을지 묻습니다. 이미 LAN에 OpenVPN(또는 유사한 것)이 설치되어 있는 경우, 일반적으로 Tailscale이 필요하지 않습니다.

전체 안내, FAQ 및 노트: [setup-home-storage/tailscale.md](./setup-home-storage/tailscale.md).

#### IPv6 활성화 (선택 사항)

**호스트 연결**에서 **활성화 IPv6**(**기본값은 꺼짐**)을 전환할 수 있습니다:

- **꺼짐:** 발견 및 연결은 IPv4 전용; IPv6 필드는 편집 시 숨겨짐; 저장된 IPv6는 유지됨
- **켜짐:** 앱이 IPv6을 스캔/작성하고 연결 후보들 사이에서 사용할 수 있음
- 원격 액세스를 위해 §4.4에서 **WebDAV / Tailscale**를 사용하십시오

---

## 5. 사진 뷰어

전체 화면으로 보려면 로컬 또는 홈 갤러리에서 사진을 탭하세요. 다른 앱(Files, Downloads, Messages 등)에서도 **Home Gallery로 열기**가 가능합니다.

### 5.1 제스처

| 동작 | 수행 내용 |
|------|------|
| 왼쪽 / 오른쪽으로 스와이프 | 이전 / 다음 사진 |
| 집게 | 확대 / 축소 (약 1×–5×) |
| 드래그 | 확대 후 팬 |
| 더블 탭 | 1×와 2.5× 사이 전환 |
| 사진을 탭하세요 | 전체 화면 전환 (도구 모음 숨기기 / 표시) |

왼쪽 하단 모서리는 사진 날짜를 표시합니다: EXIF **DateTimeOriginal / Digitized**가 존재하면 **날짜 + 시간**을 표시하고, 그렇지 않으면 수정된 날짜 / 생성된 날짜 중 **더 이른 날짜**를 사용하여 **날짜만** 표시합니다 (일반 DateTime 태그는 무시되므로 나중 파일 시간이 잘못 사용되지 않습니다). 전체 화면 또는 슬라이드쇼에서는 숨겨집니다.

### 5.2 상단 도구 모음

| 버튼 | 기능 |
|------|------|
| ← 뒤로 | 앨범으로 돌아가기 |
| 파일 이름 | 현재 사진 이름 |
| 삭제 | 현재 사진 삭제 (확인) |
| 편집(연필) | 편집기 열기 (**일부 형식만**, §5.6 참고) |

> 현재 형식을 편집할 수 없는 경우, **편집** 버튼은 표시되지 않습니다.

### 5.3 측면 / 하단 작업

화살표를 눌러 작업 행을 확장하세요(자동 축소 모드에서), 또는 설정에서 **항상 표시**로 설정할 수 있습니다.

| 동작 | 수행 내용 |
|------|------|
| 이동 | 대상 폴더를 선택한 후 이동 |
| 복사 | 대상 폴더를 선택한 후 복사 |
| Pro속성 | 파일 정보 및 EXIF |
| 이름 바꾸기 | 파일 이름 변경 |
| 공유 | 다른 앱을 통해 공유 |

> 세로: 아래쪽에 동작. 가로: 오른쪽에 동작.

### 5.4 Pro속성

속성 대화 상자가 표시됩니다:

- **파일 정보:** 이름, 경로, 크기, 유형, 생성 날짜, 수정 시간
- **EXIF** (존재할 경우): 해상도, 캡처 설정, GPS 등

EXIF 없는 사진도 파일 정보가 표시됩니다; **생성 날짜**는 항상 표시됩니다.

### 5.5 복사 / 이동

1. 복사 또는 이동을 누르세요
2. **폴더 브라우저**(전체 화면 그리드)에서 목적지를 선택하세요
- 빈 폴더는 기본적으로 **숨김** 상태입니다; 아직 파일이 없는 폴더를 선택하려면 **빈 폴더 표시**를 켜세요
- 같은 행에는 **새 폴더**와 **확인**이 있습니다
- **새 폴더** 생성 후에는 목적지가 되며(비어 있어도 표시됨) 즉시 확인할 수 있습니다
3. 목적지에 이미 같은 이름이 있는 경우:
- **덮어쓰기:** 새 파일로 교체
- **둘 다 유지:** 원본을 유지; 새 파일에는 번호(`photo (1).jpg`)가 부여됩니다
- **취소:** 중단

로컬 다중 선택: **이동**은 사진과 동영상에서 작동합니다; 선택에 동영상이 포함되면 **복사 숨김**(동영상 복사는 아직 구현되지 않음).

### 5.6 이미지 형식 및 제한

이 앱은 파일을 **보기** 또는 **편집**할 수 있는지 결정하기 위해 확장자와 MIME 유형을 사용합니다. 이 표는 **로컬 갤러리**와 **홈 앨범**에 적용됩니다(홈 저장소의 다른 파일은 일반 파일로 표시되며 뷰어에서 열 수 없습니다).

#### 볼 수 있는 사진 형식

`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.bmp`, `.heic`, `.heif`, `.svg`, `.svgz`

#### 편집 가능한 사진 형식

뷰어에서 **편집**으로 들어갈 수 있는 형식은 다음과 같으며, 저장 후에도 동일한 형식을 유지합니다:

| 형식 | 참고 |
|------|------|
| `.jpg` / `.jpeg` | 완전히 지원됨 |
| `.png` | 완전히 지원됨 |
| `.webp` | 완전히 지원됨 |

다른 보기 가능한 형식(`.gif`, `.bmp`, `.heic`, `.heif`, `.svg`, `.svgz` 등)에는 **편집 버튼이 없으므로**, 저장해도 형식을 변경하거나 파일을 손상시킬 수 없습니다.

#### SVG / SVGZ

| 주제 | 메모 |
|------|------|
| 보기 | 지원됨. 벡터는 **래스터화**되어 비트맵으로 변환됨 |
| 줌 | 극단적인 줌은 네이티브 벡터 뷰어보다 덜 선명하게 보일 수 있습니다 |
| 편집 | **지원되지 않음** (편집 버튼 없음) |
| 복잡한 SVG | 일부 효과(필터, 스크립트)가 완전히 렌더링되지 않을 수 있습니다 |
| 로컬 인덱스 | 장치는 SVG를 MediaStore에 등록해야 합니다; 시스템이 해당 파일을 인덱싱한 적이 없다면 Download에만 있는 파일은 누락될 수 있습니다 |

#### 기타 형식 참고 사항

| 형식 | 보기 | 편집 | 메모 |
|------|------|------|------|
| `.gif` | ✓ | ✗ | 정지 화면만; 애니메이션 없음 |
| `.heic` / `.heif` | ✓ | ✗ | 장치 디코딩에 따라 다름 |
| `.bmp` | ✓ | ✗ | 볼 수만 있고, 편집할 수 없음 |

#### 비디오 형식

이 앱은 **로컬 갤러리**와 **홈 앨범** 모두에서 이러한 비디오 확장자를 인식합니다. 재생 세부 사항은 §5.7에 나와 있습니다.

`.3gp`, `.avi`, `.flv`, `.m2ts`, `.m4v`, `.mkv`, `.mov`, `.mpeg`, `.mpg`, `.mp4`, `.qt`, `.rm`, `.rmvb`, `.swf`, `.ts`, `.vob`, `.webm`, `.wmv`

> 설정 → **미디어 목록 필터**에서는 참고용으로 이러한 비디오 확장자들도 나열됩니다.

### 5.7 동영상 재생

**로컬** 및 **홈 앨범** 비디오는 앱에서 스트리밍할 수 있습니다(홈 파일은 먼저 전체 다운로드할 필요가 없습니다). 슬라이드쇼는 §5.8입니다.

#### 인식된 비디오 확장자

`.3gp`, `.avi`, `.flv`, `.m2ts`, `.m4v`, `.mkv`, `.mov`, `.mpeg`, `.mpg`, `.mp4`, `.qt`, `.rm`, `.rmvb`, `.swf`, `.ts`, `.vob`, `.webm`, `.wmv`

목록에 있다는 것은 앱이 **파일을 비디오로 연다**는 것을 의미합니다. 이것이 디코딩을 보장하는 것은 아니며; 이는 여전히 내부 코덱과 장치에 달려 있습니다.

#### 전화 통화 중

| 주제 | 메모 |
|------|------|
| 엔진 | **로컬**은 **ExoPlayer (Media3)**를 선호합니다; 일부 컨테이너 / 코덱은 **libVLC**를 사용합니다. **홈 (SMB)** 일반 `.mp4` / `.mkv` / `.webm` / `.m4v` / `.3gp`는 Media3를 사용합니다; `.mov` / `.ts` / `.avi` 및 유사한 것들은 여전히 **libVLC**를 사용합니다 (컨트롤 바는 동일하게 보입니다) |
| 종종 Media3 | 로컬 및 홈 `.mp4`, `.mkv`, `.webm` 및 유사 항목; 재생 전에 짧은 코덱 프로브가 실행될 수 있음 |
| 자주 사용하는 libVLC | `.mov`, `.qt`, `.avi`, `.wmv`, `.flv`, `.mpg`, `.vob`, `.rm` / `.rmvb`, `.swf`, `.ts`, `.m2ts` 또는 하드웨어로 디코딩하기 어려운 코덱 |
| 둘 다 작동하지 않으면 | 다른 시스템 앱으로 열어 |
| 로컬 스와이프 | 로컬 갤러리/타임라인에서, 목록의 이전/다음 비디오로 왼쪽/오른쪽으로 스와이프 (Media3 경로) |
| 클립 끝 | **재생**이 제어 막대에 표시됩니다 |
| 네트워크 버퍼 | 홈 스트림은 처음 재생할 때 버퍼링될 수 있습니다; 일부 형식이나 같은 폴더에 있는 자막 파일이 더 안정적인 스트림 경로를 사용할 수 있습니다 |
| 자막 / 이어보기 / 북마크 / PiP | 동일 폴더 `.srt` / `.vtt` 는 플레이어에서 선택할 수 있습니다. 이어보기 / 북마크는 **북마크**에 있습니다. **그림 속 그림(PiP)** 은 여전히 주로 로컬 Media3를 사용합니다 |

> **코덱 제한:** 동일한 확장자가 다른 코덱을 숨길 수 있습니다. 두 엔진이 모두 실패하면 오디오만 나오거나, 검은 화면이 표시되거나, 재생이 되지 않을 수 있습니다.

#### 형식 요약

| 확장 | 인식됨 | 전화 재생 |
|--------|------|------|
| `.mp4` | ✓ | 보통 플레이함 |
| `.mkv` | ✓ | 보통 플레이함 |
| `.mov`, `.qt`, `.m4v`, `.3gp` | ✓ | 코덱에 따라 다름(종종 호환성 엔진) |
| `.avi`, `.wmv`, `.flv`, `.webm`, `.ts`, `.m2ts`, `.mpg`, `.mpeg`, `.vob`, `.rm`, `.rmvb`, `.swf` | ✓ | 코덱에 따라 다름 (libVLC) |

#### Cast 비디오를 TV에

로컬 또는 홈 앨범에서 **비디오**를 Cast하는 것은 **Basic**입니다 (사진 캐스트는 §5.9). 전화기와 TV는 동일한 LAN에 있어야 합니다. 일부 확장 기능(예: `.avi`, `.wmv`)은 Cast 버튼을 숨깁니다.

| 코덱 | 캐스팅 방식 |
|------|----------|
| **H.264 / AVC** (일반적인 휴대폰 MP4, 세로 모드 포함) | **직접 캐스트**; 비트레이트나 세로 모드 때문에 트랜스코딩하지 않음 |
| **HEVC / H.265** 및 기타 TV 비호환 코덱 | 앱이 트랜스코딩을 수행합니다 ("비디오 캐스트 준비 중…"가 표시될 수 있음) |

“**이 비디오의 비트레이트가 너무 높습니다**”라는 메시지는 **트랜스코딩이 필요하지만 홈 파일이 다운로드되지 않았거나 트랜스코드 캐시가 없는 경우**에만 나타납니다. 이로 인해 H.264가 차단되는 것은 아니라는 것이 확인되었습니다. 휴대폰에서 재생한 후, 로컬 파일이나 캐시가 존재하면 다시 캐스트를 시도하세요.

### 5.8 슬라이드쇼

로컬 또는 홈 갤러리에서 **사진**을 **전화 화면**에 자동 재생합니다. Basic 및 Pro에서 사용 가능합니다. **Pro**는 슬라이드쇼를 TV로 전송할 수도 있습니다(§5.9 참조). 비디오의 경우 비디오 플레이어를 사용하세요(§5.7).

#### 시작하는 방법

| 방법 | 주석 |
|------|------|
| **멀티 선택에서** | 길게 눌러 선택 → 하나 이상의 **사진** 선택 → **슬라이드쇼** 탭. 전체 화면 보기 **즉시 시작** |
| **뷰어에서** | 사진을 열고, **슬라이드쇼 시작** / **슬라이드쇼 일시정지**(슬라이드쇼 아이콘)을 탭하세요 |

> 선택한 항목에 **사진이 없는 경우**(예: 동영상만 있는 경우), '사진을 최소 하나 이상 선택하세요'라는 메시지가 표시되며 슬라이드쇼가 시작되지 않습니다.

#### 재생

| 주제 | 메모 |
|------|------|
| 간격 | 사진당 **약 5초** |
| 루프 | 마지막 사진 후 재생이 첫 번째 사진으로 돌아갑니다 |
| 화면에 표시 | 왼쪽 아래에는 인덱스(`3 / 12`)가 표시되고, 오른쪽 아래에는 카운트다운이 표시됩니다; 일시 정지 시 일시 정지 아이콘이 나타납니다 |
| 범위 | 다중 선택에서, **선택된 사진**만(선택 순서대로); 뷰어에서, 현재 탐색 목록에 있는 사진 |

#### 연주 중 제스처

| 동작 | 참고 |
|------|------|
| **탭** | 일시 정지 |
| **왼쪽으로 스와이프** | 다음 사진 (카운트다운 재설정) |
| **오른쪽으로 스와이프** | 이전 사진 (카운트다운 재설정) |

#### 일시 정지 후

| 동작 | 참고 |
|------|------|
| **사진을 탭하세요** | 다시 시작 (카운트다운 재시작) |
| **집기 / 끌기** | 세부 사항 확대(일시 정지 중에만 핀치 확대 가능) |
| 도구 모음 **슬라이드쇼 일시정지** | 슬라이드쇼 모드를 종료하고 일반 보기로 돌아가기 |

시스템 뒤로가기: 슬라이드쇼가 실행 중일 경우, 첫 번째 누름은 슬라이드쇼를 종료하고; 두 번째 누름은 (현재 화면에 따라) 뷰어를 종료합니다.

### 5.9 Cast 사진을 TV로

**Pro**는 로컬 또는 홈 앨범 사진을 Google Cast TV / Chromecast로 전송할 수 있습니다. Basic는 여전히 사진 뷰어에 Cast 아이콘을 표시하며, 이를 탭하면 업그레이드 안내가 나타납니다.

| 주제 | 메모 |
|------|------|
| 항목 | 사진 뷰어 제목 표시줄의 **Cast** 아이콘 |
| 연결 후 | TV가 현재 사진을 표시합니다; 휴대폰에서 핀치, 팬 및 슬라이드쇼가 TV와 동기화됩니다 |
| 슬라이드쇼 | 전화에서 시작/일시정지는 TV도 전환됩니다 |
| 시청자를 떠나다 | TV 사진 촬영이 끝나다 |
| 비디오 | 지역 / 가정 **비디오** Cast 잔여물 Basic (§5.7 참조), 사진 캐스트와 별도 |

> 전화기와 TV는 같은 LAN에 연결되어 있어야 합니다. 처음 사용할 때 지역 네트워크 권한을 허용하세요.

---

## 6. 사진 편집기

뷰어에서 **편집**을 탭하세요 (**`.jpg`, `.jpeg`, `.png`, `.webp`만 이 버튼이 표시**, §5.6 참고).

편집은 **초안**입니다. 두 개의 층이 있습니다:

1. 도구 안에서 아래쪽(세로) 또는 오른쪽(가로)을 조정합니다; **✓**를 눌러 해당 단계를 작업 미리보기에 적용하거나, **X**를 눌러 단계를 취소하고 도구 선택기로 돌아갑니다.
2. 다시 도구 선택기로 돌아가서, 상단의 **저장**은 적용된 모든 단계를 파일에 기록합니다.

> **화면 방향:** 세로 / 조정 / 자르기 모드로 들어가면 미리보기 공간을 위해 상단 제목 표시줄이 숨겨집니다; 시스템 뒤로 가기 버튼이나 툴바의 **X**를 누르면 해당 도구에서 나갑니다.

### 6.1 도구

세 가지 도구가 하단(세로) 또는 오른쪽(가로)에 나타납니다:

| 도구 | 참고 사항 |
|------|------|
| 인물 | 얼굴 매끄럽게 및 국소 색조 (밝기 / 대비 / 채도) |
| 조정 | 밝기, 대비, 채도, 색온도 |
| 자르기 | 자르기, 기울이기, 회전 / 뒤집기 |

### 6.2 자르기

어두운 패널; 아이콘 행에서 항목을 선택하세요:

| 항목 | 참고 사항 |
|------|------|
| 무료 | 모서리와 가장자리를 자유롭게 끌기 |
| 원본 | 현재 종횡비 잠금 |
| 3:2, 4:3, 16:9 | 가로형 고정 비율 |
| 2:3, 3:4, 9:16 | 세로 비율 고정 |
| 1:1 | 사각형 |

- **기울기:** −15° ~ +15°, 눈금을 드래그; **초기화**를 누르면 0으로 돌아갑니다
- **자동:** 제안된 기울기는 자동을 탭할 때만 계산됩니다 (자르기를 입력할 때는 아님)
- **뒤집기 / 회전:** 하단 확인 행 중간에 위치; 작업 중인 이미지에 즉시 적용됩니다 (이 자르기 단계를 확정하려면 나중에 ✓가 여전히 필요합니다)
- 자르기 상자를 드래그하여 이동; 모서리를 드래그하여 크기 조절. 3등분 가이드선은 드래그하는 동안에만 나타납니다
- 단계 적용하려면 **✓**을(를) 탭하세요

### 6.3 조정

어두운 패널; 확인 행의 가운데가 네 가지 항목으로 전환됩니다:

| 항목 | 비고 |
|------|------|
| 밝기 / 대비 / 채도 / 온도 | −100에서 +100까지, 중간은 0; 현재 값을 나타내는 노란색 포인터 |

- 오른쪽의 **재설정**은 현재 항목을 재설정합니다
- 회전/뒤집기에는 **자르기(Crop)**를 사용하세요
- 단계 적용하려면 **✓**을(를) 탭하세요

### 6.4 초상화

어두운 패널; 도구를 사용하면 **얼굴을 감지**합니다(‘다시 감지’할 수 있음). 확인 행이 전환됩니다: 부드럽게, 밝기, 대비, 채도.

| 제어 | 참고 |
|------|------|
| 부드럽게 | 강도 0–100; 얼굴이 있는 경우, 얼굴 크기에 따라 동적으로 제안됨 (약 20%–70%) |
| 밝기 / 대비 / 채도 | 얼굴에만 적용 (또는 수동 박스), 약 ±50% |

**얼굴을 찾을 수 없을 때:**

- 매끄러운 강도는 **0%**이며 제안이 표시되지 않습니다
- 원형 영역이 나타납니다: **한 손가락으로 드래그**하여 이동하고, **슬라이더**로 크기를 조정한 후 부드럽게 올리세요

**얼굴이 발견되었을 때:**

- 제안된 스무스는 가장 큰 얼굴이 프레임에서 차지하는 비율을 사용합니다 (가까운 샷은 더 높게, 그룹/멀리 있는 샷은 더 낮게) 그리고 '제안된 N%'를 표시합니다
- 얼굴 윤곽이 그려져 있습니다

단계를 적용하려면 **✓**를 탭하세요.

### 6.5 저장 및 취소

| 버튼 | 위치 | 비고 |
|------|------|------|
| **X** | 도구의 바닥 | 이 도구 단계를 버리고 선택자로 돌아가세요 |
| **✓** | 도구의 하단 | 도구 결과를 작업 미리보기로 병합하기 (아직 작성되지 않음) |
| **저장** | 도구 선택기 상단 | 적용한 모든 편집 내용을 파일(로컬 또는 홈 앨범)에 저장 |
| 뒤로 | 시스템 / 상단 뒤로 | 저장되지 않은 변경 사항을 버릴지 묻습니다 |

> 편집기 미리보기(도구 선택기, 세로 모드, 조정)는 **핀치 줌** 및 **더블 탭 줌**(약 1×–3×)을 지원한 후 이동할 수 있습니다; **자르기**는 미리보기를 확대하지 않으므로 제스처가 자르기 박스와 함께 유지됩니다.

---

## 7. 즐겨찾기

### 7.1 즐겨찾기 추가

로컬 또는 홈 앨범에서 도구 모음의 **북마크** 아이콘을 탭합니다. 하단에 ‘북마크됨’ 힌트가 표시됩니다.

동영상을 재생하는 동안 **별표**를 눌러 **동영상 북마크**(재개 위치 포함)를 추가하세요.

### 7.2 즐겨찾기 사용

1. 하단 바에서 **북마크**를 탭하세요 (패널을 전환하려면 해당 탭을 다시 탭하세요)
2. 패널에서 열 수 있습니다:
- **폴더 즐겨찾기:** 해당 폴더로 이동
- **비디오 북마크:** 플레이어를 열고 재개를 시도하세요

### 7.3 즐겨찾기 제거

북마크 패널에서 항목을 **길게 누르기** → 확인 (폴더와 비디오 북마크도 동일하게 작동).

---

## 8. 홈 앨범에 업로드하기

1. 로컬 갤러리에서 **다중 선택**을 입력하고 사진 / 비디오를 선택하세요
2. 하단의 **업로드** 아이콘을 탭하세요
3. **홈 앨범**(이름만)과 선택적 하위 폴더를 선택하세요(개인 백업 영역은 선택할 수 없습니다)
4. 확인

**Pro그레스**

- 진행 상황(`N / M`), 파일 이름, 바이트 막대를 보여줌
- **중지**는 남은 파일을 취소합니다; 완료된 파일은 유지됩니다; 결과에는 성공 / 일부 성공 / 취소가 구분됩니다

**같은 이름**

- 목적지에 이미 동일한 이름이 있는 경우, 업로드는 `name (1).ext`, `name (2).ext`, …로 **이름이 바뀌며**, **덮어쓰지 않습니다**

**성공적으로 업로드한 후 로컬 삭제**

- 모든 항목이 **성공적으로 업로드**되고 **휴지통**이 활성화된 경우에만, 앱이 **로컬 사본을 휴지통으로 이동할지** 묻습니다
- **휴지통으로 이동** 또는 **장치에 유지**를 선택하세요; 휴지통 끄기는 성공 메시지만 표시합니다
- 앨범 백업은 **이 기능을 제공하지 않습니다**; 휴지통으로 이동한 후에도 해당 파일이 이미 앨범 백업에 있었다면 원격 백업 사본은 여전히 유지됩니다

---

## 9. 설정

더보기 → 설정을 엽니다.

### 9.1 호스트 연결

- 저장된 **SMB 호스트** 보기, 편집 또는 삭제
- 호스트 연결 관리에서 호스트 추가 (LAN 스캔 또는 수동 주소)
- 연결 노트는 온라인 **WebDAV / Tailscale** 원격 설정 페이지를 엽니다; 동일한 페이지에서 **IPv6 활성화**를 전환할 수 있습니다(기본적으로 비활성화, §4.1 / §4.4 참고)
- 설정 홈 화면은 **호스트 SMB 추가**를 반복하지 않습니다; 관리 페이지를 사용하세요

### 9.2 홈 앨범 (설정 블록)

- 설정된 홈 앨범 수를 보여줌
- **사진 캐스트 (Pro):** 로컬/홈 사진을 TV로 캐스트합니다. Basic 스위치를 탭하면 업그레이드가 안내됩니다; Pro는 잠금 해제되어 있습니다 (뷰어 Cast 버튼 사용). 홈 **비디오** 스트리밍은 Basic이며 더 이상 설정 스위치가 필요하지 않습니다
- **세로 H.264 직접 전송:** 비디오 Cast 호환 옵션(Basic); TV 화면이 회전된 경우에만 끄세요
- **홈** 탭에서 앨범 추가/제거 (선택기/관리 화면)

### 9.3 로컬 갤러리 폴더

- **폴더 포함:** 어떤 루트를 표시할지 (기본값 `DCIM`, `Pictures`)
- **폴더 제외:** 특정 하위 폴더 건너뛰기

**Basic 대 Pro (병렬 폴더)**

| 판 | 포함할 수 있는 경로 | SAF 권한 |
|------|------|------|
| **Basic** | MediaStore 루트: `DCIM`, `Pictures` (동영상도 `Movies`) 및 그 하위 폴더; **추가로** 병렬 폴더 | **폴더 선택 및 권한 부여(읽기/쓰기)** 필요 |
| **Pro** | Basic와 동일 | Basic와 동일 |

Basic와 Pro은 설정에서 `Documents`와 같은 병렬 경로를 허용할 수 있습니다; 그 권한이 없으면 복원은 `Pictures/…`로 매핑될 수 있습니다(§9.7 참조).

**참고 (안드로이드 플랫폼 제한):** Android 11에서 시스템은 폴더 선택기로 전체 `Download` 루트를 승인하는 것을 **허용하지 않습니다**—“이 폴더 사용” 버튼이 회색으로 표시됩니다. 이는 앱 버그가 아닙니다. `Documents`(또는 유사한 항목)을 승인하거나, Download 아래에 **하위 폴더**를 만들고 해당 하위 폴더를 승인하세요.

**두 보조금을 혼동하지 마세요**

| 보조금 | 사용 용도 |
|------|------|
| **폴더 선택 및 권한 부여 (SAF)** | 병렬 폴더 자체를 읽기/쓰기 (복원, 복사/이동 **안으로**) |
| **미디어 관리 (설정 → 로컬 갤러리)** | `DCIM` / `Pictures`과 같은 표준 앨범의 원본을 삭제하거나 수정할 수 있습니다. 카메라 폴더에서 병렬 폴더로 이동할 때 원본 삭제를 요청할 수 있으며, 미디어 관리 접근 후에는 보통 그 알림이 반복되지 않습니다 |

### 9.4 디스플레이

| 설정 | 메모 |
|------|------|
| 세로 그리드 열 | 세로 사진 한 줄당 사진 수 |
| 가로(grid) 열 | 가로 사진 행당 수 |
| 폴더 이름 크기 | 루트에 있는 폴더 제목 크기 |
| 뷰어 작업 행 | 자동 축소 / 항상 표시 |

### 9.5 캐시

캐시 크기를 확인하고 **캐시 지우기** (네트워크 썸네일, 이미지 디스크 캐시, 네트워크 비디오 스트림 캐시, 메모리 캐시). 원본은 손대지 않습니다; 다음 홈-앨범 탐색 시 캐시를 다운로드하거나 재구성해야 합니다.

### 9.6 미디어 목록 필터

로컬 및 홈 앨범 목록에서 폴더 이름, 파일 이름 또는 확장자를 숨깁니다. 내장된 사진/비디오 확장자 규칙은 읽기 전용 참조입니다.

> 설정은 다음과 같이 그룹화됩니다: 로컬 갤러리, 미디어 필터, **호스트 연결**, 홈 앨범(네트워크 비디오 스트리밍 포함), 앨범 백업, 캐시, **문제 보고 / 진단 내보내기** 등.

### 9.7 앨범 백업

앨범 백업은 로컬 갤러리에서 홈 저장소의 개인 영역(`.HomeGallery/backup/{device}/`)으로 **한 방향으로** 업로드되므로 전화기 실패 후 복원할 수 있습니다.

각 장치는 자체 폴더(모델, 선택적 표시 이름, 마지막 백업 시간)에 `device_info.json`을 저장합니다. 여러 사람이 동일한 백업 앵커를 공유할 때는 앨범 백업 설정에서 **로컬 백업 ID → 표시 이름**을 설정하세요; “기존 백업 연결” 목록에는 `android_xxxx`만 표시되는 대신 해당 이름/모델이 나타납니다.

| 주제 | 메모 |
|------|------|
| **방향** | **전화 → 집 저장** (일방향) |
| **Basic / Pro** | **Basic:** 수동 백업, 폴더 제외, 오래된 전화 복원 / 연결. **Pro:** 선택적 **자동** 백업 Wi‑Fi |
| **자동 백업** | **Pro 전용.** Basic은 수동으로 폴더를 백업하고 제외할 수 있으며, 백그라운드 동기화는 없음. 활성화된 경우(그리고 “Wi‑Fi 전용” 조건이 충족되면), 홈 Wi‑Fi에 연결하면 **곧 스캔/업로드가 예약**됨; 앱은 또한 약 6시간마다 그리고 갤러리가 변경될 때 확인함. 배터리 절약 모드/아직 준비되지 않은 NAS는 이를 지연시킬 수 있음—최선의 노력, 2차 수준 아님 |
| **폴더 제외** | **Basic / Pro:** 로컬 하위 경로 건너뛰기(예: `DCIM/Screenshots`); 이미 인덱싱된 항목은 큐에서 제외 |
| **휴지통으로 이동** | 홈 백업을 **삭제하지 않음**; 또한 **기기에서 누락됨**도 아님 (휴지통에서 로컬 파일 복원) |
| **이 앱에서 영구 삭제** | **기기에서 누락됨** 아님; 다음 백업에서 해당 `.HomeGallery` 파일을 삭제하고 추적을 중단함 |
| **앱 외부로 이동됨** | “기기에서 누락됨”으로 표시됨; 홈 사본은 수동 복원 또는 정리를 위해 보관됨. **보류 중인 백업 확인**이 NAS에 연결된 후, 백업 파일이 이미 사라진 항목은 목록에서 제거됨 (복원 불가한 유령 없음) |
| **복원** | **백업/복원 보기**에서 항목을 **수동으로** 선택하고 로컬에 다시 기록 |
| **제공되지 않음** | **양방향 동기화** — NAS에서 백업 파일을 직접 변경/삭제해도 휴대폰 갤러리가 **업데이트되지 않습니다** |

만약 **다른 공유로 전환**하거나, **호스트를 변경**하거나, **백업 앵커 폴더를 변경**하면, 로컬 추적이 다시 시작됩니다: 동기화됨 / 실패 / 백업 전용 개수가 초기화됩니다. **보류 중인 백업 확인**을 눌러 다시 스캔하세요 (새 대상에 이미 이 장치의 백업이 있는 경우, 항목이 전체 재업로드 없이 동기화된 것으로 맞춰질 수 있습니다).

Basic '공용 폴더에 업로드'와 달리, 백업 존은 앱에서 관리하며 기본적으로 일반 네트워크 탐색에서는 숨겨져 있고, 기본적으로 로컬 사본을 유지합니다.

**백업 영역 탐색(큰 폴더 격자)**

1. **더 보기 → 앨범 백업** → **백업 영역 탐색**
2. 홈 앨범 폴더 모드(대형 그리드)에서 개인 백업 루트를 열고, 필요에 따라 폴더를 탐색합니다
3. **백업 전용** 항목은 빨간 테두리가 있습니다. 길게 눌러 선택한 후 백업 구역에서 제거하고 추적도 삭제할 수 있습니다.
4. 백업 디스크에 존재하지만 **로컬 인덱스 행이 없는** 파일은 **추적되지 않음**으로 표시됩니다(보통 수동으로 복사되었거나 인덱스 재설정 후); Backup-only로 표시되기 전에 스캔/원격 인덱스를 실행하세요
5. 목록 스타일 복원 / 필터는 여전히 아래의 **보기 백업 / 복원**을 사용합니다

**복원 (R1.1)**

1. **더 보기 → 앨범 백업** (또는 설정의 앨범 백업) → **백업 / 복원 보기**
2. 기본 필터 **기기에서 누락됨** (이전에 백업되었지만, 로컬에서는 사라짐; 휴지통의 항목이 아니며, 이 앱에서 영구 삭제된 항목도 아님)
3. **모든 누락 복원**을 탭하거나, 여러 항목을 선택한 후 **선택 항목 복원** → 확인(파일 수, 크기, **경로 매핑 수**) → 복원
(오래된 전화기를 연결한 후의 아이템은 보통 “백업 전용”이며; 기기에서 누락된 필터는 대기 중인 파일도 표시합니다)
4. 복원된 파일이 로컬 갤러리에 나타납니다

**병렬 폴더 복원 경로(중요)**

백업에 `Documents/…`:과 같은 병렬 경로가 포함되어 있는 경우

| 조건 | 복원이 쓰는 위치 |
|------|------|
| 그 평행 경로에 대한 SAF 없이 | **`Pictures/{original relative path}`**에 매핑됨 (예: `Documents/Office Lens/a.jpg` → `Pictures/Documents/Office Lens/a.jpg`; 비디오는 `Movies/…` 아래에 매핑될 수 있음) |
| 해당 병렬 폴더에서 **선택 및 승인** | **원래 상대 경로**로 다시 기록 (Pictures에 매핑되지 않음) |

Basic와 Pro 모두 병렬 폴더를 승인할 수 있습니다(설정 → 로컬 갤러리 폴더 참조).

확인 대화 상자가 경로 매핑을 표시하면 일부 파일은 `Pictures` 아래에 저장됩니다. 장치 간 복원(R2)도 동일한 규칙을 사용합니다.

**새 전화기 (R2)** (기존 백업 연결 / 전체 복원 라이브러리; **Basic 및 Pro**)

1. **더보기 → 앨범 백업** → **백업 보기 / 복원**
2. **기존 백업 연결**을 탭 → 이전 전화기의 `backup/{deviceId}/`를 NAS에서 선택 (**복원 소스만**)
3. 인덱싱 후, 이 전화에 없는 파일에 대해 **백업 전용** 필터를 사용하세요
4. 다중 선택 또는 **누락된 모든 항목 복원** (같은 병행 폴더 규칙: SAF 없이 `Pictures/…`으로 이동)
5. 이 전화기의 나중 백업은 여전히 **자신의** 폴더에 기록됩니다; 이전 전화기는 별도로 백업을 계속할 수 있습니다

**앱 저장 공간을 지운 후(같은 전화기)**

1. 백업 후, 먼저 **설정 내보내기** (이것은 백업 `deviceId`도 기억합니다)
2. 앱 저장 공간을 지우기(또는 재설치) → 권한 다시 부여 → **가능한 한 빨리 설정 복원** (먼저 앨범 백업을 활성화하지 마세요, 그렇지 않으면 새로운 기기 아이덴티티가 생성될 수 있습니다)
3. 앨범 백업 열기 → **보류 중인 백업 확인**: 앱이 홈 백업 존에 연결하고 NAS과 여전히 일치하는 로컬 파일을 동기화된 것으로 표시합니다—**전체 재업로드 없음**
4. 이미 다른 ID를 가진 **다른 전화기**에서 설정을 복원하면: 기록된 ID는 유지되며, 가져온 이전 `deviceId`는 연결된 복원 소스로 처리됩니다(색인/복원하려면 “기존 백업 연결” 사용).
5. 내보내기가 오래된 경우(`deviceId`이 없음), 백업 후 다시 내보내거나 "기존 백업 연결"로 오래된 폴더를 선택하세요

### 9.8 내보내기 및 설정 복원

| 주제 | 메모 |
|------|------|
| **내보내기 설정** | 로컬 갤러리, SMB 호스트, 홈 앨범, 즐겨찾기 및 백업 환경설정을 Download/Home Gallery로 내보냅니다 |
| **자격 증명** | 이 파일에는 **암호화된** 형태(일반 텍스트가 아니며 강력한 암호화도 아님)로 SMB개의 비밀번호가 포함되어 있습니다; 비공개로 유지하십시오—공용 클라우드에 업로드하지 마십시오 |
| **설정 복원** | 최근 내보내기 또는 JSON 파일을 선택하세요; 이렇게 하면 현재 설정과 자격 증명이 **덮어쓰기**됩니다 |

> 이전 리팩터링 전의 오래된 (`SambaProfile`) 테스트 빌드를 사용한 경우, **앱 데이터를 지우거나 재설치**한 후, 호스트와 홈 앨범을 다시 설정하세요. 이전 형식의 설정 파일은 가져오지 마세요.

### 9.9 문제 및 진단 보고

| 주제 | 메모 |
|------|------|
| **문제 신고** | 이메일을 열며 선택적으로 버전 및 기능 플래그 요약 포함 |
| **진단 내보내기** | 사진 내용이나 SMB 비밀번호 없이 `diagnostics-*.zip`(장치 정보, 최근 오류, `last_crash.txt` 등)을 생성합니다 |
| **충돌 후** | **앱을 다시 열기** 전에 내보내기 하여 ZIP에 최신 치명적 충돌 스택 트레이스가 포함되도록 합니다 |

---

## 10. 자주 묻는 질문

### Q1: 로컬 사진이 없나요?

- **사진 및 동영상** 접근 권한이 허용되었는지 확인
- 설정 → 로컬 갤러리 폴더: 폴더가 **포함된 폴더**에 있는지 확인하세요

### Q2: 집 저장소에 연결할 수 없나요?

- 전화기와 NAS / PC는 동일한 집 Wi‑Fi / LAN에 있어야 합니다
- 설정 → **호스트 연결**: 주소, 계정, 비밀번호 확인
- NAS / PC(포트 445)에서 SMB가 활성화되어 있는지 확인
- 스캔에서 아무것도 발견되지 않으면: Wi‑Fi을 켜고 스토리지와 같은 LAN에 머문 후 다시 스캔하세요
- **멀리 있을 때 연결할 수 없음:**
- NAS: 원격 액세스는 **WebDAV** 이어야 하며, 올바른 DDNS / 포트 → [WebDAV guide](./setup-home-storage/webdav.md)
- Windows / 없음 WebDAV: [Tailscale guide](./setup-home-storage/tailscale.md)을(를) 참조하세요
- 기타 경우: [Troubleshooting §E](./setup-home-storage/troubleshooting.md)

### Q3: 로컬 사진을 삭제하거나 편집할 때 시스템 권한 요청이 나타나나요?

Android 11+에서, 다른 앱에서 생성된 일부 미디어는 수정/삭제하려면 추가 권한이 필요합니다. 시스템 대화상자에서 허용하세요.

### Q4: 초상화 / 매끈한 모습이 약해 보이나요?

- 얼굴이 감지되었는지 확인하거나, 원을 끌어다 놓고 슬라이더로 크기를 조절하세요
- **스무스** 올리기
- 적용 (✓) 후 저장

### Q5: 편집 후 품질이 떨어지나요?

인물 **미리보기**는 속도를 위해 낮은 해상도를 사용합니다; **저장**은 원래 해상도로 처리합니다. 여전히 효과가 너무 강하면, 부드러움(Smooth)을 낮추세요.

### Q6: 홈 앨범으로 복사/업로드가 느린가요?

속도는 네트워크 대역폭과 파일 크기에 따라 달라집니다. 안정적인 Wi‑Fi 연결을 사용하세요.

### Q7: 왜 SVG는 볼 수 있지만 편집할 수 없는가?

SVG는 벡터입니다. 이 앱은 벡터를 편집하고 SVG로 저장할 수 없고, 래스터화된 비트맵만 **볼** 수 있으므로 편집은 숨겨져 있습니다. §5.6을 참조하세요.

### Q8: GIF / HEIC에 편집 버튼이 없는 이유는 무엇인가요?

이 형식들은 볼 수는 있지만, 편집된 저장본은 원래 형식(GIF 애니메이션, HEIC 인코딩)을 유지할 수 없습니다. 손상을 방지하기 위해, 편집은 `.jpg`, `.jpeg`, `.png`, `.webp`에만 해당됩니다.

### Q9: 로컬 갤러리에서 SVG 파일이 누락되었나요?

Android **MediaStore**가 이를 인덱싱했는지 확인하세요 (Download초가 일반적인 경우입니다). 시스템이 SVG를 미디어로 등록하지 않았다면, 이 앱은 이를 목록에 표시할 수 없습니다.

### Q10: 영상이 목록에 있지만 재생되지 않나요?

- 확장이 §5.7에 있는지 확인하십시오
- 휴대폰에서: 앱이 내장 엔진(Media3 또는 호환성 엔진)을 시도합니다. 실패하면 **다른 앱**으로 엽니다. 동일한 파일을 다른 플레이어에서 테스트할 수도 있습니다.
- 네트워크 비디오: 네트워크를 확인하세요; 필요하면 설정에서 캐시를 지우고 다시 시도하세요

---

## 11. 권한

| 권한 | 이유 |
|------|------|
| 사진 및 동영상 (READ_MEDIA_IMAGES / READ_MEDIA_VIDEO) | 로컬 사진 및 동영상 읽기 |
| 인터넷 (INTERNET) | 홈 SMB 저장소에 연결 |
| 네트워크 상태 (ACCESS_NETWORK_STATE) | 연결 상태 감지 |
| Wi‑Fi 상태 (ACCESS_WIFI_STATE) | LAN 검색 |

---

## 12. Basic 그리고 Pro

Home Gallery는 **Basic**와 **Pro**를 제공합니다. Basic는 **광고가 없으며** 이미 전체 사진/비디오 관리 기능을 포함하고 있습니다(로컬 및 홈 앨범, 타임라인, 홈 비디오 스트리밍, 휴지통, 병렬 폴더, 수동 앨범 백업 및 제외 폴더, 이전 전화 복원/연결 등). 추가 공간은 클라우드 할당량이 아닌 홈 스토리지에서 제공합니다. **Pro**는 **선택적 일회성 구매**로, 추가로 **자동** 앨범 백업(Wi‑Fi에서 백그라운드 업로드) 및 TV로의 **사진 캐스팅** 기능을 해제합니다.

| 판 | 역할 |
|------|------|
| **Basic** | 전체 로컬 + 홈 미디어 관리; 수동 백업, 폴더 제외, 복원 |
| **Pro** | Basic의 모든 것 + **자동 백업** (백그라운드 Wi‑Fi 동기화) + **포토 캐스트** (TV) |

> Pro덕션 빌드는 인앱 구매를 통해 Pro를 잠금 해제합니다; 디버그 빌드는 테스트를 위해 설정에서 관련 기능을 전환할 수 있습니다.

### 12.1 Basic — 무료

| 기능 | 주석 |
|------|------|
| 로컬 갤러리 | 탐색, 타임라인, 검색, 정렬; 병렬 폴더 (SAF) |
| 홈 앨범 | 호스트, 사진 및 **타임라인**, 북마크; **홈 비디오** 스트림 |
| 사진 관리 | 복사, 이동, 이름 바꾸기, 삭제, 공유 |
| 사진 편집기 | 자르기, 조정, 초상화 등 |
| 업로드 | 로컬 사진/동영상을 **홈 앨범** 폴더에 업로드 |
| **앨범 백업** | **수동** 단방향 백업; **폴더 제외**; **복원** / 이전 전화 연결; **자동** 백그라운드 동기화 없음 |
| **휴지통** | 설정에서 활성화; 삭제된 항목이 휴지통으로 이동하고 복원될 수 있음 |
| 로컬 / 홈 비디오 | 재생 및 Cast (장치에 따라 다름) |
| 슬라이드쇼 | 휴대폰에서 사진 자동 재생 (§5.8 참조) |
| 사진 캐스트 (TV) | **Pro 필요** (§5.9 참조) |
| 설정 | **더 보기 → 설정**: 호스트, 갤러리 범위, 캐시, 등등 |

### 12.2 Pro — 일회성 구매

Basic의 모든 것 위에:

| 기능 | 주석 |
|------|------|
| **자동 백업** | Wi‑Fi에서 **자동** 단방향 백업(백그라운드 일정); 수동 백업, 폴더 제외, 복원은 Basic과 동일 |
| **사진 캐스트** | Cast 로컬 / 가정 사진 및 슬라이드쇼를 TV로 (핀치, 팬; §5.9 참조) |

### 12.3 관련 설정 스위치

| 어디 | 노트 |
|------|------|
| 설정 → **홈 앨범** → 포토 캐스트 | **Pro:** 사진 / 슬라이드쇼를 TV로. Basic 스위치를 탭하면 업그레이드 안내 |
| 설정 → **홈 앨범** → 초상화 H.264 직접 전송 | 비디오 Cast 호환성 (Basic) |
| 설정 → **앨범 백업** | 백업 활성화, 폴더 제외, **자동 동기화** (자동 동기화는 Pro만 해당) |
| 설정 → **로컬 갤러리** → 휴지통 | 휴지통 및 기본 삭제 활성화 (Basic 및 Pro) |

성공적인 Pro 구매/복원 후, 한 번만 “**당신은 Pro에 있습니다**” 대화 상자가 나타납니다; 이후 실행 시(프로세스가 종료된 후를 포함) 다시는 나타나지 않습니다.

**휴지통 옵션** (설정 → 로컬 갤러리 → 휴지통 옵션): 기본 삭제는 **휴지통으로 이동** 또는 **영구 삭제**; 휴지통 비우기 전에 확인; 만료 알림 (시스템이 약 하루 후 영구 삭제할 항목에 대한 일일 알림; Android 13+는 알림 권한 필요).

- **기본값 = 휴지통으로 이동:** 로컬 갤러리/뷰어에서 삭제하면 휴지통으로 이동(복원 가능).
- **기본값 = 영구 삭제:** 확인 후, 로컬 갤러리/뷰어에서 삭제하면 실제로 삭제되며, 그 삭제로 인해 **휴지통에 들어가지 않아야 합니다**. 휴지통에는 여전히 다른 이유로 항목이 표시될 수 있습니다:
1. **완전히 성공적으로 업로드**한 후, 로컬 사본을 휴지통으로 이동하도록 선택했습니다(기본 삭제 설정과 무관하게)
2. **사진 편집**은 덮어쓰기 전에 휴지통에 편집 전 백업을 보관합니다
3. **다른 앱 / 시스템 갤러리**가 항목을 공유 MediaStore 휴지통으로 이동했습니다
- 휴지통이 **꺼져** 있으면, 로컬 삭제는 항상 영구적입니다; 업로드가 성공해도 복사본을 휴지통으로 옮길지 묻지 않습니다.

### 12.4 업그레이드 방법

1. **더보기 → 설정 → Pro로 업그레이드** (또는 자동 백업, 사진 캐스트 등 시도할 때 나타나는 업그레이드 안내)
2. Google Play 일회성 구매를 완료하세요
3. 새 전화기나 재설치 후에는 설정에서 **구매 복원**을 사용하세요

### 12.5 Pro 자주 묻는 질문

**Q: Basic가 편집, 삭제 및 업로드를 할 수 있나요?**
네. Basic는 단순 보기용 앱이 아니라 완전한 사진 관리 프로그램입니다.

**Q: 로컬 또는 홈 비디오에 Pro가 필요합니까?**
아니요. 로컬 및 홈 비디오 재생은 Basic입니다. Pro은 **자동** 앨범 백업과 **포토 캐스트**를 잠금 해제합니다.

**질문: 사진을 TV로 캐스팅하려면 Pro가 필요합니까?**
네. Basic는 휴대폰에서 슬라이드쇼를 재생할 수 있습니다; TV 캐스트는 Pro가 필요합니다. 비디오 Cast는 Basic로 남아 있습니다.

**Q: 비디오 캐스트에서 '비트레이트가 너무 높음'이라고 나오는 이유는 무엇인가요?**
트랜스코드가 필요하지만 로컬 파일이나 트랜스코드 캐시가 아직 없는 경우에만. 일반적인 휴대폰 **H.264**는 직접 전송되며 비트레이트에 의해 차단되지 않습니다. HEVC 및 기타 TV 호환되지 않는 코덱은 먼저 준비됩니다. §5.7을 참조하십시오.

**질문: 업로드는 백업과 어떻게 다른가요?**
업로드는 파일과 **홈 앨범** 폴더(Basic 및 Pro)를 선택하는 것입니다. **휴지통이 켜져 있는 경우**, 완전히 성공한 업로드는 공간을 절약하기 위해 로컬 사본을 휴지통으로 이동할 수 있습니다. 백업은 로컬 갤러리를 앱 관리 **개인 백업 영역**에 **단방향으로** 기록하고 기본적으로 로컬 사본을 유지합니다; 필요할 때 앱에서 복원할 수 있습니다.

**Q: 앨범 백업은 Basic과 Pro에서 어떻게 다릅니까?**
Basic은 **수동** 백업, **폴더 제외**, 그리고 구형 전화 복원 / 연결을 지원합니다. **Pro**은 추가로 Wi‑Fi에서 **자동** 백그라운드 백업을 활성화할 수 있습니다.

**Q: 백업은 양방향인가요?**
아니요. 백업은 **폰 → NAS**입니다. NAS에서 백업 파일을 편집해도 폰에 다시 기록되지 않습니다. 복원은 앱에서 의도적으로 수행하는 동작입니다.

**Q: Office Lens 파일은 Documents에 있습니다. Basic이 그것들을 볼 수 있나요?**
예. Basic와 Pro은 설정 → 로컬 갤러리에서 `Documents`와 같은 병렬 폴더를 **선택하고 승인할** 수 있습니다.

**Q: 복원할 때, Documents 및 다른 병렬 폴더들은 어디로 가나요?**
그 폴더에 대해 SAF 없이: `Pictures/` 아래의 **매핑된 경로** (`Documents/…`로 돌아가지 않음). 해당 병렬 폴더가 허용된 경우: 원래의 상대 경로. 복원 확인 대화상자에는 매핑된 경로 수가 표시됩니다. §9.7을 참조하세요.

**Q: Download 권한을 허용할 때 '이 폴더 사용'이 회색으로 표시되는 이유는 무엇인가요?**
이것은 **Android 11+ 시스템 제한**입니다. 앱은 Download **루트**에 대한 트리 권한을 얻을 수 없습니다. `Documents`를 선택하거나 Download 아래의 **하위 폴더**를 승인하세요. 앱은 “읽기 전용으로 Download 포함” 경로를 제공하지 않습니다(복사/복원 쓰기와 충돌하고 더 불편하기 때문입니다).

---

## 13. 버전 정보

**더보기 → 정보** (또는 **더보기 → 설정 → 정보**)에서 다음을 볼 수 있습니다:

- 버전 (현재 **1.2.3**; 앱의 **정보**를 신뢰하세요)
- 제작일
- **사용자 안내서** (이 온라인 매뉴얼: https://jasoncschang-stack.github.io/homegallery-privacy/help/)
- 문제 신고
- 저작권

설정 → **지원**을 선택하면 동일한 안내가 열립니다.

이 공개 가이드는 앱 **1.2.3**과 매뉴얼 **1.48**에 맞습니다.
